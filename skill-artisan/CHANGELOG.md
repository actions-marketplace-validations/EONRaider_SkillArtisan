# Changelog

All notable changes to SkillArtisan are documented here. Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/); versioning follows [Semantic Versioning](https://semver.org/), interpreted for this project as:

- **Major version bump (1.0.0 → 2.0.0)** marks a v-stage boundary, not just a breaking change — v1 → v2 is the deliberate release split (see the master spec's "Build Stages → Plugin Files → Release Scope"), not an accident of semver discipline.
- **Minor version** for new capability within a stage that doesn't break existing usage (e.g. adding cross-agent evaluation as an opt-in mode within v1).
- **Patch version** for fixes — corrected patterns, tightened validation, documentation accuracy.

## [2.10.0] - 2026-09-22

### Added
- **Per-fact dedup check while drafting a new skill's body**, complementing (not replacing) the Decision Gate's whole-skill check 2. Motivated by a real downstream miss: a justified new skill's first draft restated a CI-job-to-local-command mapping that a sibling skill in the same skills directory already owned; the drafting pass didn't catch it, an independent adversarial review did, later. Check 2 structurally couldn't have — `dedup_search.py --query` scores frontmatter descriptions only, and the mapping lived in the sibling's body. `SKILL.md`'s "Authoring a new skill" step 4 now names the check explicitly: before a drafted paragraph or table restates a governance, tooling, CI, or convention fact, search the sibling skills for it and point at the existing home instead of copying it. `references/writing-philosophy.md`'s "Content hygiene" gains the matching "One home per fact" rule with the why (two copies drift; a reader can't tell which is current).
- **`dedup_search.py --fact "<fact>" --siblings-of <draft-skill-dir>`**: a fact-level mode over sibling SKILL.md *bodies*. Splits each body into blocks (paragraphs, tables, lists, fenced code kept whole across inner blank lines), tags each with its nearest heading, and ranks by coverage — the fraction of the fact's terms a block contains — rather than Jaccard, since a one-line fact against a multi-row table would otherwise score near zero for the table's size alone. Keeps two-letter terms (CI, PR, gh) that the whole-skill tokenizer drops, since exactly those carry the facts this mode looks for; `--query` tokenization is unchanged. `--siblings-of` searches the draft's parent directory instead of the default paths and excludes the draft itself; reports `SKILL.md:line`, heading, and matched terms. Same shortlist-not-verdict contract as `--query`. Covered by a new `tests/test_dedup_search.py` (the script's first dedicated tests), including a regression fixture rebuilt from the incident's shape: a sibling whose description never mentions CI, with the mapping table in its body.
- **`creating-skills/evals/evals.json` eval 7** covers the drafting flow: the user explicitly asks for a CI-mapping table a sibling already owns; the expected behavior is to point at the sibling and say why, without reopening the agreed "create new" verdict.

## [2.9.2] - 2026-09-20

### Fixed
- **`security_scan.py`'s pattern checks (`--verbose`) flagged its own pattern-definition literals as HIGH findings when scanning itself**: `security_scan.py` ships inside `creating-skills`'s `scripts/`, so a normal audit of that skill scans this file against itself — and `DANGEROUS_CODE_PATTERNS`/`INTERACTIVE_INPUT_PATTERNS`/`HTTP_URL_PATTERN`'s own regex source and example strings necessarily contain, as literal text, the exact code shapes they're built to detect (e.g. the line defining the `os.system(` check contains the text `os.system(`). Reproduced directly by calling `run_pattern_checks()` on this repo, not assumed: 3 HIGH findings (`dangerous-code-pattern` ×2, `blocking-interactive-input` ×1) plus 1 MEDIUM (`insecure-http-url`, `HTTP_URL_PATTERN`'s own regex source matching its own check). Fixed with `self_scan_exempt_line_numbers()`, the same marker-bracketed-range idea `docstring_line_numbers()` (above) already uses, but locating the range via a pair of `# self-scan-exempt:start`/`:end` sentinel comments bracketing just the pattern-definition block, and scoped to files literally named `security_scan.py` — every other file, including the rest of `security_scan.py` itself (CLI parsing, the gitleaks invocation, etc.), is still scanned exactly as before. Separately, `pr_execute.py`'s ordinary `for prefix in ("git@github.com:", "https://github.com/", "http://github.com/")` URL-prefix parsing loop (`normalize_repo_slug`) tripped the `email-address`/`insecure-http-url` heuristics on the same non-sensitive, example-format strings; fixed by adding `github.com` to the existing `EMAIL_EXCEPTIONS`/`HTTP_URL_EXCEPTIONS` allowlists (MEDIUM-only, informational, never blocking). Regression tests added to `tests/test_security_scan.py`: the real `security_scan.py` source produces zero findings against itself, the same literal pattern in a differently-named file is still caught, a genuine dangerous pattern placed outside the exempt range in `security_scan.py` is still caught, and both the `github.com` exemptions and real email/`http://` findings elsewhere are verified in both directions.

## [2.9.1] - 2026-09-20

### Fixed
- **`security_scan.py`'s pattern checks (`--verbose`) flagged this plugin's own security-education prose as HIGH findings (issue #20)**: `references/security-checklist.md` documenting `os.system(`/`pickle.load(` as things the `dangerous-code-pattern` check catches, and `references/writing-philosophy.md`'s own "never do this" `C:\Users\...` example for `absolute-user-path`, both inside inline `code spans` — a self-referential false positive, not a hypothetical one, confirmed via `audit.py report` against this repo's own `creating-skills/`. Unlike the Phase 4 audit-pilot false positives deliberately left unfixed (`benchmark/audit-pilot/RESULTS.md`'s Phase 4 section — distinguishing a real call from a descriptive *label* inside actual Python source is unreliably ambiguous), this is a different, lower-risk signal: markdown's own inline-code/fenced-block syntax already unambiguously marks "this is shown, not live," the same signal `validate.py`'s `check_path_references` already relies on for the identical false-positive shape. Fixed with the same mechanism, scoped to `.md` files only (a `.py`/`.sh` file has no such convention, and bash backticks mean command substitution — a real risk, not documentation): `strip_markdown_code()` blanks fenced code blocks and inline code spans while preserving line numbers, applied before the `absolute-user-path`, `dangerous-code-pattern`, `unsafe-command-interpolation`, `email-address`, and `insecure-http-url` checks. Also closes a previously-unreported MEDIUM `insecure-http-url` false positive on the same `security-checklist.md` line, caught incidentally while verifying the fix. A genuinely dangerous pattern written outside a code span in a `.md` file is still caught — verified directly, not just by inspection. Regression tests added to `tests/test_security_scan.py` covering both directions (suppressed inside code spans, still flagged outside them) plus a `.py`-file control case.

## [2.9.0] - 2026-09-20

### Added
- **Skill-name resolution**: `audit.py report` and `validate.py` now accept a bare installed skill name, not just a path (`_common.py`: `default_skill_search_roots`, `SkillResolutionError`, `resolve_skill_by_name`, `resolve_skill_path_or_name`). Searches cwd walked up through every parent's own `.claude/skills/`, plus `$CLAUDE_CONFIG_DIR`'s `skills/`, `plugins/cache/`, and `plugins/marketplaces/`. Never guesses on 0 or 2+ matches — raises with every candidate listed for the caller to disambiguate with a real path instead. Ported from a separate personal skill's linter (`skill-audit`), built on top of the existing `find_skill_dirs` rather than a second directory walker.
- **`validate.py` closes a real path-portability gap**: `check_claude_skill_dir_refs` catches a dangling `${CLAUDE_SKILL_DIR}/...` reference (including inside `allowed-tools` frontmatter) that the existing `check_path_references` structurally cannot see — that check strips fenced code/inline spans before its regex runs, and `${CLAUDE_SKILL_DIR}` references overwhelmingly live inside exactly that. WARN-level, not a hard error: a skill's own documentation *about* this convention is a legitimate false positive this check can't structurally rule out. `check_scripts_need_bash_permission` (also WARN) flags a bundled script mentioned in the body with no `Bash` granted in `allowed-tools` — every invocation would hit a permission prompt.
- **`audit.py` gets a broader prose-density scorer**, `check_prose_density`, alongside (not replacing) the existing narrow `check_degrees_of_freedom_proxy`: case-insensitive absolute-word counting plus ALL-CAPS "shouting" word detection (with an acronym whitelist), tiered lean/medium/heavy. The whitelist was calibrated against this project's own `creating-skills/SKILL.md` — an earlier version flagged this project's own PASS/FAIL/WARN/MANUAL checklist vocabulary and MCP/AI/CLI/POST as "shouting."
- **Four judgment-lens `MANUAL_ONLY_ITEMS`** backed by a new `references/audit-judgment-lenses.md`: `prose-should-be-script`, `prose-should-be-hook-or-disallowed-tools`, and `condensed-version-proposed` are new; `inline-vs-fork-decision-correct` is upgraded in place (not duplicated) to point at worked fork-appropriateness criteria. Adapted from the same personal skill's audit process — this plugin's `agents/analyzer.md` was considered and rejected as the home for these, since both its roles require a comparison pair (blind A/B winner/loser, or cross-eval pattern-surfacing) and explicitly forbid suggesting skill improvements for one of them; "read one skill cold and judge it" needed the existing `MANUAL_ONLY_ITEMS` mechanism instead.
- **`references/writing-philosophy.md`** documents the Claude Code re-attachment mechanic (first ~5000 tokens re-attached after compaction, 25,000-token shared budget) that `check_body_size`'s exact threshold has silently encoded without explanation until now.
- Explicitly **not** ported from the source skill: secret-scanning (redundant — `security_scan.py` already covers this with gitleaks + patterns), the flat frontmatter allow-list (redundant — this project's tiered `PORTABLE_FIELDS`/`CLAUDE_CODE_ONLY_FIELDS`/`THIRD_PARTY_FIELD_FAMILIES` is strictly more capable), gerund-naming checks (unrelated, already a separately-tracked Gap Table item).

### Fixed
- Two findings from a `solid-coding` review of this PR's own new code, both closing the same class of hazard already fixed once in this diff for `missing_references`: `script-references-need-bash-permission` now reads a structured `result["bash_permission_warning"]` key instead of substring-matching "Bash"/"permission" in free text; `default_skill_search_roots` now resolves `$CLAUDE_CONFIG_DIR` to an absolute path (a relative value previously produced non-absolute search roots, silently inconsistent with `resolve_existing_dir`'s guarantee).

### Notes
- Verified via self-validation: `validate.py`/`audit.py` run against this plugin's own `creating-skills/` throughout development, catching two real false positives before merge (an acronym-whitelist gap in `check_prose_density`, described above) rather than after. 216-test suite passes, verified against real Python 3.8 as well as the dev environment's 3.10.

## [2.8.1] - 2026-09-20

### Fixed
- **Internal quality pass over `skill-artisan/scripts/*.py`** via a full `solid-coding` audit (5 principle-cluster reviews, every finding independently adversarially re-verified before applying). Refactor only — no observable behavior change for any existing caller; `184`-test suite passes before and after every increment, verified against real Python 3.8 as well as the dev environment's 3.10. OCP/pattern-fit, LSP/ISP/DIP, and Law-of-Demeter/CQS reviews came back with zero findings (mostly free functions over dicts/Paths, already table-driven where it matters — a legitimate clean result, not a shallow pass). Four findings confirmed real and fixed:
  - **`_common.resolve_existing_dir()` replaces 6 copies of the identical "not a directory" CLI check** across `audit.py` (3 sites), `validate.py`, `security_scan.py`, and `gha_audit.py` — same stderr wording and exit code everywhere, previously drifting by copy-paste. `pr_execute.py`'s similar-looking but genuinely different git-repo check is correctly left alone.
  - **`_common.frontmatter_and_body()` replaces a byte-identical frontmatter/body delimiter scan** duplicated in `audit.py`'s `get_body` and `validate.py`'s `validate()` — the same failure mode (independently-drifting frontmatter parsers) that previously caused a real production bug (a 704-char description silently reading as empty). Both edge cases (missing opening/closing delimiter) verified identical before and after.
  - **`validate.validate()` now exposes `missing_references`/`missing_references_error` as structured keys**, instead of `audit.py`'s `check_frontmatter_and_paths` classifying a `path-references-exist` vs. `frontmatter-valid` finding by string-matching `"Missing referenced files"` in free text. This codebase already guards the analogous "gerund" substring hazard with an explicit comment and test; this closes the same class of gap here. A new test monkeypatches the message wording entirely and confirms classification still works.
  - **`audit.py`'s three inline third-party N/A-reframing blocks (issue #4) consolidated into one table-driven `apply_third_party_reframing()`.** Adding a future 4th pipeline-artifact-only check no longer requires hand-writing a new inline special case. Table rows use per-item match/detail-builder callables, not a shared status flag, specifically because the three original blocks weren't uniform — `evals-present` only reframes the exact "artifact absent" case, never a present-but-malformed `evals.json` (a real content defect). Adversarial verification caught that a naive status-only table would have silently widened `evals-present`'s reframing to cover malformed-JSON cases too; a new regression test (`test_malformed_evals_json_still_fails_for_a_third_party_skill`) pins the previously-uncovered case directly.

### Notes
- Deferred: nothing — the two `PLAUSIBLE`-verdict findings above were adversarially re-verified against `principle-tensions.md`, revised to a right-sized fix, and applied; nothing was rejected outright, but both were genuinely reworked from their initial proposed shape rather than rubber-stamped (see each bullet above for what changed). `184`-test suite passes (174 baseline + 10 new).

## [2.8.0] - 2026-09-20

### Added
- **Standard repository scaffolding**: `.github/CODEOWNERS`, `CODE_OF_CONDUCT.md` (Contributor Covenant v2.1), `.github/ISSUE_TEMPLATE/` (an `audit-gap` form modeled on this repo's actual, 100%-self-filed issue history, a generic `bug_report` form, and a `config.yml` redirecting security reports to GitHub Security Advisories per `SECURITY.md`), `.github/PULL_REQUEST_TEMPLATE.md`, and `.github/dependabot.yml` (scoped to the `github-actions` ecosystem only — there's no pip dependency file anywhere in the repo to track otherwise).
- **`.github/workflows/tests.yml`**: this repo's Python test suite (`skill-artisan/tests/`) and a linter now actually run in CI on push/PR. Previously the only two workflows (`self-test-action*.yml`) exercised the GitHub Action against fixtures, `workflow_dispatch`-only by design — real unit-test execution had no CI coverage at all despite 172+ tests existing. This new workflow deliberately never invokes `uses: ./` (the audit action), so it doesn't touch the existing "never audit ourselves automatically" decision.
- **`pyproject.toml`** (root): `[tool.ruff]` config only — no `[project]`/`[build-system]` table, since this repo deliberately isn't a pip-installable package. `select = ["E", "F"]`; `E501` (line length) and `I` (import sorting) are both off — `E501` because this codebase's docstrings/comments are long by a deliberate content-density choice, `I` because most of `tests/` deliberately inserts `sys.path` entries before importing local modules, a pattern isort's sorting doesn't understand and would misflag.
- CI status badge added to the root README, alongside the existing release-version badge.

### Fixed
- Five small pre-existing lint findings surfaced by introducing `ruff` (unused imports in `benchmark/harness/report.py` and `tests/test_disable_model_invocation.py`, two extraneous f-string prefixes, and one genuinely dead local variable in `description_optimizer.py`'s skill-publishing path — a leftover from before the hidden-staging-name/atomic-rename approach replaced it). No behavior change; the dead variable was never read anywhere in the file.
- **The README's stated `Python 3.8+` requirement was never actually true for 5 test files**, surfaced immediately by `tests.yml` actually running the suite on 3.8 for the first time (`test_aggregate_findings_source.py`, `test_audit_references_toc.py`, `test_disable_model_invocation.py`, `test_security_scan.py`, `test_third_party_mode.py`): each used PEP 604/585 syntax (`str | None`, `list[dict]`) in a function signature without `from __future__ import annotations`, which only became valid without it in 3.10+. Fixed by adding the same future-import every `scripts/*.py` module already uses, matching the existing convention rather than rewriting the signatures with `typing.Optional`/`typing.List`. Verified directly against real Python 3.8.20 (not just inferred from the CI failure), not just the CI logs: 174/174 tests pass.

### Notes
- Branch protection on `master` is a repo-settings change, not a file, and is left as a manual follow-up now that `tests.yml` exists to require as a check — not something this change enables on its own.
- 174-test suite passes; `ruff check .` passes clean.

## [2.7.0] - 2026-09-20

### Added
- **Three new `THIRD_PARTY_FIELD_FAMILIES` entries close out audit-gap issues #8 and #9.** `routing-metadata: {triggers}` (issue #8) — `triggers` was originally grouped with `command`/`agents`/`compatible_tools` as a "bespoke one-off convention," but five independent authorship models (Anthropic's own `anthropics/knowledge-work-plugins` zoom-plugin, `mims-harvard/tooluniverse`, `aitytech/agentkits-marketing`, and two more) converged on it as a real, coherent routing convention, corroborated across Phases 8, 10, and 16. `governance-metadata: {owner, service, reviewed}` and `tool-usage-metadata: {tools}` (issue #9, `nvidia/skills` Phase 9) — a real ownership/review-date triplet and a separate tool-cataloging list, found on 35 skills in one corpus. `tool-usage-metadata` is proposed here as the new family's name; issue #9 explicitly left it open for maintainer confirmation.
- **Hard guardrail, moved to sit directly above `PORTABLE_FIELDS`:** `tools` must never be added to `PORTABLE_FIELDS` or aliased to `allowed-tools` — unlike `tools`, `allowed-tools` has real runtime permission-bypass semantics, and `PORTABLE_FIELDS` feeds `run_skills_ref()`'s portable-field passthrough. Originally drafted as a comment on the `tool-usage-metadata` entry itself; relocated during review to sit next to the declaration it actually protects, so an editor touching `PORTABLE_FIELDS` directly sees the warning locally instead of needing to find it ~50 lines away. Regression test added (`tests/test_field_classification.py::test_tools_never_treated_as_portable_or_aliased`) asserting `tools` is absent from `PORTABLE_FIELDS` and never aliased inside the new family.
- Regression tests added: `test_issue_8_and_9_families_classify_as_known_third_party` (all three new families classify correctly, none falls through to `unknown`) and the guardrail test above. `test_bespoke_conventions_still_error` updated to drop `triggers` from its hard-error tuple, since it now has a resolved home.

### Notes
- Both fixes close audit-gap issues #8 and #9. The five other field-family candidates surfaced only in issue #9's comment thread (`type`, a `progressive_disclosure` cluster, a 10-field marketing family, `requirements`, and a governance taxonomy) stay deliberately unresolved — each is a single-corpus data point, and this project's standing discipline is to wait for independent corroboration rather than guess. 174-test suite passes.

## [2.6.1] - 2026-09-20

### Fixed
- **`SECURITY.md`'s Supported Versions table still read `2.5.x (latest)` after the `2.6.0` release.** Nothing had bumped the table since it was first filled in at `2.5.12`. Fixed to `2.6.x (latest)` / `< 2.6`; the policy prose itself (single actively-developed line, no LTS branches, private-advisory-only reporting) was already correct and is unchanged.
- **`CONTRIBUTING.md`'s "Repo layout" tree and prose omitted the root `README.md` entirely**, stating `README.md` "exist[s] only inside `skill-artisan/` — one canonical copy... not duplicated at root." This was accurate before `2.4.5` but not after: a substantial root `README.md` (the project's front door) has existed since that release. Fixed by adding it to the layout tree and clarifying the actual relationship — root `README.md` is why-this-exists/install/GitHub-Action framing for someone deciding whether to use the project; `skill-artisan/README.md` is the live plugin doc for someone who already has, and explicitly defers back to root rather than repeating it. This is not the drift-risk duplication the file correctly warns against for `CHANGELOG.md`.

### Notes
- Pure documentation-accuracy fixes; no code changed. 172-test suite passes (unchanged).

## [2.6.0] - 2026-08-21

### Added
- **A new checklist item, `authoring-template-detected`, catches author-provided skill-authoring templates before they draw a misleading rebuild/FAIL verdict (issue #12).** Six real instances confirmed across the audit pilot (Phases 13, 19×2, 20, 22, 24): a fill-in-the-blank scaffolding skill (Mustache placeholders, bracketed `[TODO:...]` markers, or an explicit self-declaring description like "you should never use this skill directly, it is just a template") gets discovered and audited as ordinary content, then FAILs `frontmatter-valid`/`description-pushy-imperative` and gets graded as broken — a category error, since the content is *supposed* to look unfilled. A `find_skill_dirs`-level directory-name exclusion was considered and rejected: 102 real skills across two corpora legitimately use `templates/skills/<name>/` as genuine storage, so excluding by directory name would silently drop real content (see `benchmark/vendored/README.md`'s Phase 19 entry). This fix instead adds a narrow, content-based check — template syntax in the `name` field, or one of a handful of specific verbatim phrasings pulled from the six real instances in the `description` field — that suppresses the upgrade-vs-rebuild verdict without touching discovery at all, so real content can never be silently dropped. A full sweep against all ~10,200 skills already vendored across the pilot found exactly six hits (the six known instances, zero false positives) plus a **seventh, previously unknown instance** sitting mis-graded in `nickcrew/claude-cortex` since Phase 15 — caught for the first time by this check. Regression tests added (`tests/test_authoring_template_stub.py`, using the real corpus frontmatter blocks, including three real-skill negative cases to confirm the heuristic doesn't sweep up genuine content about templates/TODOs).
- **`aggregate_findings.py` now accepts `--source {auto,first-party,third-party}`, forcing `audit.py`'s source resolution for every skill in a bulk-audit run (issue #11).** Fourteen real skills across ten-plus independent authors (Phases 16–29) converged closely enough on this pipeline's own `evals.json` shape that `detect_source()`'s auto-detection misclassified them as first-party, drawing bogus `evals-present`/`security-scan-marker-current`/`lifecycle-classified` findings. Rather than weaken `detect_source()` itself — both of the alternatives considered (drop `evals.json` from auto-detection; require a corroborating artifact) reopen issue #4's original unfalsifiability trap, where a fresh first-party draft with real evals but no security-scan marker yet would silently skip its own gate — the fix accepts the auto-detection's residual false-positive rate as the right trade for `audit.py`'s single-skill use case, and instead closes the actual operational gap this pilot hit every phase: a bulk audit of a *known* third-party corpus can now pass `--source third-party` up front instead of eating a bogus-FAIL correction after the fact. Regression tests added (`tests/test_aggregate_findings_source.py`), using a fixture with the real fluxcd/petekp/bmad-labs/atlassian-shaped `evals.json` (the sub-case with zero discriminator signal available).

### Notes
- Both fixes close out audit-gap issues #11 and #12 — the two design-tradeoff items this pilot had deliberately left open pending enough real-world corroboration to resolve safely rather than guess. 172-test suite passes.

## [2.5.13] - 2026-08-21

### Fixed
- **`audit.py`'s `has_lifecycle_markers` metadata-field check matched any unrelated tag containing the substring "lifecycle".** The `metadata` frontmatter field is reconstructed as one flat string by `_common.py`'s parser (no real YAML nesting), so `"lifecycle" in metadata.lower()` matched any tag/category anywhere in the block — found live during audit-pilot Phase 23 on two real skills in `terminalskills/skills`: `mlflow` (tagged `ml-lifecycle`, about MLflow's own ML-lifecycle management) and `sequenzy-email-marketing` (tagged `lifecycle-email`, about email-campaign lifecycles), both misdetected as first-party and drawing bogus `evals-present`/`security-scan-marker-current` FAILs. Fixed with the same co-occurrence discipline the body-text path already uses: the metadata field now requires "lifecycle" alongside "timelessness" or one of the two documented lifecycle categories (`capability-uplift`, `encoded-preference`) before counting as a marker — a real classification always names its category, so this doesn't weaken genuine detection, verified against both a synthetic true-positive metadata marker and the existing body-text fixture. Regression tests added (`tests/test_lifecycle_metadata_false_positive.py`, using the real corpus frontmatter blocks); 153-test suite passes; both real skills re-verified as correctly `third-party` after the fix.

### Notes
- Phase 23 of the audit pilot: 1,215 skills audited across 20 repos, headlined by `terminalskills/skills` (1,018 skills vs. 1 sitemap-listed — the pilot's largest single hidden collection yet, audited in full across 11 chunks with zero per-skill errors). Full narrative: `skill-artisan/benchmark/audit-pilot/RESULTS.md`'s Phase 23 section.

## [2.5.12] - 2026-08-21

### Added
- **`SECURITY.md` filled in.** The repo-root file existed only as GitHub's unedited template (placeholder supported-versions table, placeholder reporting instructions). Now documents the actual policy: latest `2.5.x` line only receives fixes, reports go through GitHub's private vulnerability reporting (now enabled on the repo) rather than a public issue, and scope is stated explicitly — the plugin's own scripts/agents/Action code are in scope, skills a user produces and then edits/publishes themselves are not (that responsibility is already covered by `references/sanitization-checklist.md`).

## [2.5.11] - 2026-08-21

### Fixed
- **`audit.py`'s `description-pushy-imperative` check is English-only no longer.** Tracked as issue #10 since Phase 11: `TRIGGER_FRAMING_RE` (looks for literal English "use when/if/for/during" phrasing) and the 40/100-character length floors are both calibrated for English text density — confirmed wrong two independent ways on two independent CJK corpora, verified against real descriptions both times, not assumed from the aggregate rate. Korean sentences carrying genuine trigger-framing content structurally can't match an English regex (`nomadamas/k-skill`, Phase 11: 77% FAIL/WARN). Short Chinese descriptions carry as much semantic content as much longer English ones but get FAILed by a character-count floor built for English word length (`chaterm/terminal-skills`, Phase 16: 98%). Rather than guess at per-language regex or rescaled thresholds with no linguistic verification — real risk of encoding a wrong pattern with false confidence, worse than an honest gap — a predominantly non-Latin-script description (≥30% non-Latin letters, threshold picked empirically: genuinely-English descriptions in CJK-authored repos measure 0%, genuine CJK-script descriptions measure a 47–67% median across both corpora) now reports **MANUAL** ("cannot verify from files alone") instead of a confident FAIL/WARN the check structurally can't evaluate. Re-run against both corpora: Korean's 91 FAIL/WARN drops to 74 MANUAL + 22 WARN (the 22 remaining are genuinely English-written descriptions in the Korean repo, correctly still scored); Chinese's 62 FAIL drops to 45 MANUAL + 17 FAIL (the 17 remaining are genuinely thin even translated — `Linux file and directory operations`-style entries — correctly still flagged). Regression tests added (`tests/test_description_cjk_manual.py`, using the real corpus strings verified in RESULTS.md, not synthetic examples); 146-test suite passes.

### Notes
- Issue #10 closed. Issue #11 (fluxcd/agent-skills' independent convergence on this pipeline's evals.json schema) stays open per the standing discipline — one data point isn't enough to pick between its three options without risking the same speculative-guess problem this fix avoided.

## [2.5.10] - 2026-08-20

### Fixed
- **`audit.py`'s first-party auto-detection collided with the Anthropic/daymade skill-creator lineage's evals schema.** `detect_source()` accepted any `{"evals": [...]}` wrapper in `evals/evals.json` as proof a skill entered this repo's own pipeline — but that wrapper is also the skill-creator tool family's documented schema (`skill_name`/`evals[]` with per-eval `assertions` and `files`; this pipeline's schema in `creating-skills/references/schemas.md` writes `expectations` instead). Found in the wild during audit-pilot Phase 15: two skills in `5dive-ai/skills` (a random low-star draw) carried skill-creator-authored evals, flipped to first-party, and drew three bogus pipeline-artifact FAILs each — the same collision class the `.security-scan-passed` marker check already guarded against (85 daymade skills ship a same-named plain-text marker), now proven for the second artifact type. Fixed: an eval entry set carrying `assertions` with no `expectations` anywhere is another pipeline's artifact → third-party; a prompts-first draft with neither field is byte-identical in both pipelines and still counts as first-party, preserving issue #4's anti-unfalsifiability design for fresh drafts. Regression test added (`tests/test_third_party_mode.py`); 136-test suite passes; the affected corpus re-audited clean (all 14 5dive skills third-party, 0 bogus FAILs).

### Notes
- Phase 15 (pilot) of the real-world audit pilot: 979 skills audited across 28 repos drawn from skills.sh's low-sitemap-count tier — the deliberately lower-quality population the original screening excluded, selected by a live-measured funnel (fresh 20,000-URL sitemap crawl → 1,730-candidate tier → batched GraphQL license/star screen → seeded stratified draw across four star bins). 100% verification survival; the funnel's license projections held (69% viable vs. ~72% projected); the tier definition itself leaks (two hidden mega-collections supplied 89% of the skills, incl. a 717x sitemap undercount — the pilot's worst ever); and the cohort's quality gradient inverted the plan's expectation: high-star projects' incidental skills were the *cleanest* slice, hobbyist collections the worst. Content-as-data discipline stated and applied throughout; nothing manipulative found. **5,142 skills now audited across the whole pilot.** Full narrative: `skill-artisan/benchmark/audit-pilot/RESULTS.md`'s Phase 15 section.

## [2.5.9] - 2026-08-20

### Fixed
- **`_common.find_skill_dirs`' fixed-pattern-list architecture replaced with a bounded recursive walk.** `bobmatnyc/claude-mpm-skills` discovered only 2 of 174 real skills — not a missing pattern, but proof the pattern-enumeration model (ten fixed glob patterns accumulated since Phase 3) had reached a shape of problem it can't solve: plain category nesting at arbitrary depth, with no `skills/` marker directory anywhere to anchor a fixed-depth pattern against. Replaced with `base.rglob("SKILL.md")`, keeping the exact same symlink-skip and intermediate-directory-exclusion logic. Verified safe before switching, not assumed: run head-to-head against all nineteen corpora already vendored at the time — identical results everywhere except two, both confirmed improvements (172 real `bobmatnyc` skills recovered; `aws-agent-toolkit-for-aws` naturally recovers a sub-package skill Phase 9 had deliberately left undiscovered). Also measurably faster on the largest corpus tested (817 skills: 0.079s → 0.048s). Full 135-test suite passes unchanged.

### Notes
- Phase 13 of the real-world audit pilot: 361 skills audited across the dev-tooling cluster trim (`secondsky/claude-skills`, `bobmatnyc/claude-mpm-skills`). Found and documented (not code-fixed, by design) a real design-philosophy tension: `bobmatnyc` declares its own explicit multi-tier `progressive_disclosure`/`token_estimate` content architecture, which produces a genuinely higher `rebuild`-decision rate under SkillArtisan's own body-size heuristic — a real disagreement between two legitimate approaches, not a bug on either side. Also a fifth corroboration of the field-family pattern on [#9](https://github.com/EONRaider/SkillArtisan/issues/9) (`progressive_disclosure` and six sibling fields, 174/174). Full narrative: `skill-artisan/benchmark/audit-pilot/RESULTS.md`'s Phase 13 section.

## [2.5.8] - 2026-08-20

### Fixed
- **`_common.find_skill_dirs` missed 43 real skills in `adobe/skills`**: a `plugins/<product>/<version>/skills/<category>/<name>/SKILL.md` shape one wildcard deeper on both sides of the literal `skills` component than the existing nested pattern, e.g. `plugins/aem/6.5-lts/skills/aem-workflow/workflow-triaging/SKILL.md`. Checked for collisions against every vendored corpus before adding the new pattern: zero matches anywhere else. 162 of 162 now discovered.
- **A fifth exclusion-worthy intermediate directory, `example`, added to `find_skill_dirs`**: `flutter/agent-plugins` bundles two deliberate test fixtures for its own SKILL.md linter under `tool/dart_skills_lint/example/skills/{valid,invalid}/SKILL.md`, both explicitly self-described as fixtures in their own body text. Checked across every corpus audited so far before adding `example` alongside the existing `assets`/`tests`/`fixtures` exclusions: these two are the only matches anywhere.

### Notes
- Phase 12 of the real-world audit pilot: 464 skills audited across the second vendor wave (`adobe/skills`, `google/skills`, `dotnet/skills`, `grafana/skills`, `flutter/agent-plugins`), completing the 10-repo first-party-vendor cluster begun in Phase 9. `dotnet/skills` initially looked like a second discovery gap but turned out to be the Phase 10 `assets`/`tests`/`fixtures` exclusion working correctly on a brand-new corpus (real test fixtures for dotnet's own skill-validator tooling), not a new bug. Also documented (not code-fixed, by design): one genuine `path-references-exist` false positive with no safe general pattern (an unfenced literal placeholder in checklist prose), and two genuine true positives — real broken content in `dotnet/skills` and `grafana/skills`' own documentation. Full narrative: `skill-artisan/benchmark/audit-pilot/RESULTS.md`'s Phase 12 section.

## [2.5.7] - 2026-08-20

### Fixed
- **`check_path_references`' sixth false-positive mechanism, the first to be repaired rather than skipped**: `trailofbits/skills` uses a `{baseDir}`-prefixed template-variable convention (`{baseDir}/references/quick-reference.md`) for cross-skill-relative links — a real, cross-platform convention (not Claude Code's own `${CLAUDE_SKILL_DIR}` syntax), verified exhaustively (41 of 41 instances across 7 skills resolve to real files once the prefix is stripped, not sampled). Unlike the existing `SKIP_PREFIXES` entries (`http://`, `https://`, `mailto:`, `#`, `~` — none locally verifiable), `{baseDir}` references are real and checkable, so the fix strips the prefix and re-checks existence rather than skipping validation outright — a genuinely broken `{baseDir}/nonexistent.md` reference is still correctly caught. Regression tests added to `tests/test_validate.py` covering both directions.

### Notes
- Phase 11 of the real-world audit pilot: 197 skills audited across `nomadamas/k-skill` (the pilot's first non-English corpus, Korean) and `trailofbits/skills` (a named professional security-research firm, CC-BY-SA-4.0 licensed — audited under an "audit yes, adapt no" policy). Found and documented (not code-fixed, by design) a genuine, two-dimensional English-bias bug in `description-pushy-imperative` — a literal-English-phrase regex and a length threshold calibrated for English text density, both confirmed against real Korean descriptions, FAILing/WARNing 77% of the corpus. Tracked as [#10](https://github.com/EONRaider/SkillArtisan/issues/10), left for a deliberate design decision rather than a speculative regex guess. Full narrative: `skill-artisan/benchmark/audit-pilot/RESULTS.md`'s Phase 11 section.

## [2.5.6] - 2026-08-20

### Fixed
- **`_common.find_skill_dirs` had a real false-positive collision with its own Phase 9 pattern**: `mims-harvard/ToolUniverse`'s `skills/create-tooluniverse-skill/assets/skill_template/SKILL.md` — a fill-in-the-blanks skill-creation template (`name: tooluniverse-[domain-name]`) — structurally matched the `skills/*/*/*/SKILL.md` pattern added in v2.5.5, even though it's bundled reference content, not a real skill. This is the same false-positive class first suspected in v2.5.4 (`alirezarezvani`'s `assets/sample-skill/`, `skillforge`'s `tests/fixtures/sample-skill/`) — both already excluded by accident (no pattern reached their depth), not by design. Now confirmed a third time and reachable by a real pattern, so it needed one. Fixed with a general rule: `find_skill_dirs` now excludes any match whose path passes through a directory literally named `assets`, `tests`, or `fixtures` — justified by three confirmed instances across three corpora, checked against every vendored corpus before landing (exactly one real false positive fixed, zero legitimate discoveries excluded anywhere). Regression test added to `tests/test_common_find_skill_dirs.py`.

### Notes
- Phase 10 of the real-world audit pilot: 327 skills audited in `mims-harvard/ToolUniverse` (Harvard's Zitnik Lab), the academic/research authorship corpus, largest single repo in the current roadmap. Also documented (not code-fixed, by design): a genuine content-drift finding in the source repo itself (41 skill names have two non-identical surviving copies across its three packaging layers, one missing a real `disable-model-invocation: true` field), and a third independent corroboration of the `triggers:` field on [#8](https://github.com/EONRaider/SkillArtisan/issues/8). Full narrative: `skill-artisan/benchmark/audit-pilot/RESULTS.md`'s Phase 10 section.

## [2.5.5] - 2026-08-20

### Fixed
- **`_common.find_skill_dirs` missed 101 real skills in `aws/agent-toolkit-for-aws`** (Phase 9 of the audit pilot) — a literal top-level `skills/` collection directory (not preceded by any wildcard, unlike every other pattern) with two or three category levels of nesting beneath it before the skill's own directory (`skills/core-skills/amazon-bedrock/SKILL.md`, `skills/specialized-skills/database-skills/rds-db2/SKILL.md`). Checked for collisions against every other vendored corpus before adding the two new patterns: zero matches anywhere else. 150 of 151 skills now discovered; the last (a doubly-nested sub-package shape, `plugins/aws-agents/skills/agents-pay/packages/openclaw/skills/agents-pay/SKILL.md`) is real content left as a documented, known gap rather than a fifth pattern justified by one example. Regression tests added to `tests/test_common_find_skill_dirs.py`.

### Notes
- Phase 9 of the real-world audit pilot: 616 skills audited across three corporate-DevRel-vendor repos (`nvidia/skills`, `forcedotcom/sf-skills`, `aws/agent-toolkit-for-aws`). One new `audit-gap` issue opened ([#9](https://github.com/EONRaider/SkillArtisan/issues/9), two new NVIDIA frontmatter field families — `owner`/`service`/`reviewed` and `tools`, the latter flagged with an explicit warning against ever aliasing it to the security-relevant `allowed-tools` field). One genuine, reproducible content defect documented in NVIDIA's own `doca-*` skill family (systematic off-by-one relative links), not a SkillArtisan fix. Full narrative: `skill-artisan/benchmark/audit-pilot/RESULTS.md`'s Phase 9 section.

## [2.5.4] - 2026-08-20

### Fixed
- **`_common.find_skill_dirs` missed 79 real skills across two new first-party corpora** (Phase 8 of the audit pilot, `anthropics/knowledge-work-plugins` and `anthropics/financial-services`) — two genuine discovery-pattern gaps, not previously seen: `financial-services` wraps the already-supported `category/plugin/skills/<skill>` convention in one more top-level `plugins/` directory (117 of 118 skills missed entirely); `knowledge-work-plugins`' `zoom-plugin` nests real, independently-frontmattered platform-variant *sub-skills* one or two levels beneath an already-discovered skill directory, explicitly routed to from the parent skill's own body text. Verified the new patterns don't collide with two known false-positive shapes already present in other vendored corpora (`alirezarezvani`'s `assets/sample-skill/`, `skillforge`'s `tests/fixtures/sample-skill/`) before adding them. Regression tests added to `tests/test_common_find_skill_dirs.py`, including negative tests for both false-positive shapes.
- **`check_path_references`' fifth false-positive mechanism**: link-syntax examples inside HTML comments (`<!-- ... [intro](URL) ... -->`) — an author-facing authoring note, not a real reference — found 12 times across five plugin-specific copies of `anthropics/claude-for-legal`'s `cold-start-interview`. Same root-cause family as the four mechanisms already fixed (fenced code blocks, inline code spans, `~/`-prefixed cross-skill references). Fixed by stripping HTML comments before the link scan runs, alongside the existing fenced-block/inline-span stripping. Regression test added to `tests/test_validate.py`.

### Notes
- Phase 8 of the real-world audit pilot: 430 skills audited across three first-party Anthropic repos (`anthropics/knowledge-work-plugins`, `anthropics/claude-for-legal`, `anthropics/financial-services`) — the pilot's first vendor/first-party-authored source. One new `audit-gap` issue opened ([#8](https://github.com/EONRaider/SkillArtisan/issues/8), reconsidering the `triggers:` frontmatter field for `THIRD_PARTY_FIELD_FAMILIES` given new first-party corroboration). Full narrative: `skill-artisan/benchmark/audit-pilot/RESULTS.md`'s Phase 8 section.

## [2.5.3] - 2026-08-20

### Fixed
- **The test suite's `REPO_ROOT = parents[2]` convention broke ~15 tests when run from an installed plugin copy**, not just the dev checkout — flagged, but deliberately left unfixed, at the end of the prior live-install check. Every test file computed `SCRIPTS_DIR = REPO_ROOT / "skill-artisan" / "scripts"` assuming a full monorepo layout (`<monorepo-root>/skill-artisan/tests/test_x.py`, two parents up is the monorepo root); `EONRaider/claude-plugins`' `git-subdir` source (`path: "skill-artisan"`) fetches only that directory's contents, so in an install the same test file sits one level shallower and the re-appended `"skill-artisan"` segment resolves nowhere. New shared `tests/_repo_paths.py` computes `PLUGIN_ROOT` as the test file's grandparent directory directly — always correct in both contexts, since that's exactly what `git-subdir`'s `path` fetches — and detects `MONOREPO_ROOT` (`None` in an install) for the handful of files genuinely outside the plugin (`action.yml`, the monorepo's own root `README.md`, the pre-2.5.1 root `marketplace.json`), which now `self.skipTest(...)` with a stated reason instead of erroring when those don't exist. All 15 affected test files updated to import from it. Verified three ways, not just by inspection: full suite from the dev checkout (121 pass), from a constructed installed-copy layout (116 pass, 5 skip with named reasons), and — the real test — copied into the actual `~/.claude/plugins/cache/eonraider/skillartisan/2.5.2/` directory `claude plugin install` had populated from the live marketplace and run from there (same 116/5 split).

## [2.5.2] - 2026-08-20

### Fixed
- **`creating-skills` failed its own `frontmatter-valid` check** — found by a live end-to-end check: `claude plugin marketplace add EONRaider/claude-plugins` → `claude plugin install skillartisan@eonraider` against the real marketplace, then running `audit.py report` on the installed skill exactly the way `SKILL.md` tells Claude to audit any skill. `frontmatter-valid` FAILed: the skill's own `compatibility` field was 530 characters, over skills-ref's 500-character hard limit. This had shipped since `ac282ce` (2026-08-19) with zero coverage — nothing in the 119-test suite ever ran `validate.py`'s real skills-ref check against `creating-skills/SKILL.md` itself, only against fixtures standing in for it. Trimmed the field to 491 chars (dropped redundant wording, no information lost — the surfaces/requirements list is unchanged). Regression test added: `tests/test_creating_skills_self_validates.py`, which calls `validate.validate()` against the real shipped skill directory, closing the exact "only tested via fixtures" gap that let this through.

## [2.5.1] - 2026-08-20

### Fixed
- **The self-hosted marketplace declaration is gone — distribution moved to the dedicated `EONRaider/claude-plugins` repo.** `[2.3.0]` introduced a root `.claude-plugin/marketplace.json` naming this repo the `eonraider` marketplace, explicitly "chosen as a shared distribution channel for future plugins beyond this one" — but when that future plugin arrived (`EONRaider/foreman`), it self-declared the SAME marketplace name from its own repo, and only one `eonraider` marketplace can be registered per Claude Code install. The canonical manifest now lives in `EONRaider/claude-plugins`, which lists both plugins by external `git-subdir` reference (this repo: path `skill-artisan`, ref `master`); this repo keeps only the plugin-level `skill-artisan/.claude-plugin/plugin.json`. `README.md`'s install line changed accordingly: `claude plugin marketplace add EONRaider/claude-plugins` (the `claude plugin install skillartisan@eonraider` line is unchanged — the marketplace *name* is the same, only its host repo moved). Release-mechanics consequence: the version now lives in **two** files (plugin.json + README's two pins), not three, and every release must also bump the `sha` pin in `claude-plugins`' entry for installs to pick it up. `tests/test_marketplace_manifest.py` is replaced by `tests/test_plugin_manifest.py` — the cross-repo half of the old sync invariant can't be tested from here, so the successor guards what remains local: plugin.json validity, the README version pins, that the install instructions point at the unified marketplace, and that a root marketplace.json never silently comes back to recreate the collision.

## [2.5.0] - 2026-08-20

### Added
- **`audit.py` now understands skill provenance** — resolving [#4](https://github.com/EONRaider/SkillArtisan/issues/4), the architectural parent of the audit pilot's deferred gaps. Three checklist items (`evals-present`, `security-scan-marker-current`, `lifecycle-classified`) verify artifacts only this plugin's own pipeline produces, and FAILed on effectively 100% of the 1,418 third-party skills audited across all six pilot corpora regardless of actual quality — `RESULTS.md`'s stated cost was that an outside author "would reasonably conclude the tool doesn't understand their skill wasn't authored for it." `report` and `bulk` now take `--source {auto,first-party,third-party}` (default `auto`), and in third-party mode those three items report a new fifth status, **`N/A`** — the detail still says what was observed (and what adopting the skill into the pipeline would add), but the item is excluded from the pass-rate denominator alongside WARN/MANUAL. Detection is deliberately "ANY pipeline artifact present → first-party" rather than requiring all three: requiring all would make the checks unfalsifiable, since a fresh first-party draft missing an artifact would silently reclassify as third-party and skip the exact gate those items enforce — `--source first-party` is the documented override for artifact-less drafts. Detection also demands *this pipeline's* marker shape (JSON with a content hash), because end-to-end verification against the vendored daymade clone found 85 skills shipping a same-named plain-text `.security-scan-passed` from a different tool, which existence-only detection misclassified — caught before commit precisely because verification ran against real corpora, not just fixtures. Verified effect: daymade's `competitors-analysis` goes from 12/16 (75%) with 3 artifact FAILs to 12/13 (92%) with 3 informative N/A lines; a bulk run over `obra/superpowers` resolves source per skill in one pass. `pr-plan`, which builds contribution plans from FAIL items only, automatically stops proposing SkillArtisan-pipeline artifacts to upstream maintainers — a correctness fix it inherited for free. New fixture `tests/fixtures/third-party-fixture/` and regression tests in `tests/test_third_party_mode.py`; the pilot-era `KNOWN_BOILERPLATE_IDS` stopgap in `aggregate_findings.py` is left as-is (historical pilot tooling, its comment already points here).
- **`validate.py` now recognizes known third-party metadata field families** — resolving [#7](https://github.com/EONRaider/SkillArtisan/issues/7). mukul975/Anthropic-Cybersecurity-Skills' consistent framework-mapping taxonomy (`mitre_attack`, `nist_csf`, `domain`, `subdomain`, `author`, `tags`, `version`, plus per-skill `d3fend_techniques`/`mitre_f3`/`atlas_techniques`/`nist_ai_rmf`) hard-FAILed `frontmatter-valid` on **817 of 817** skills — a real, coherent taxonomy, not sloppy authoring, and exactly the "general answer rather than field-by-field allowlisting" the pilot notes called for. `THIRD_PARTY_FIELD_FAMILIES` maps family names (`security-framework-taxonomy`, `common-authoring-metadata`) to their fields; `classify_extended_fields` gained a third bucket, and family fields now produce a portability *warning* (naming the family and recommending `metadata:` nesting for spec compliance) instead of an error. Deliberately NOT admitted, so they keep hard-erroring: the recorded true positives and bespoke one-off conventions from Phases 5-6 — `user_invocable` (underscore typo for the real `user-invocable`), `triggers`, `command`, `agents`, `compatible_tools`. Knock-on effect, intended and verified: `audit.py`'s `frontmatter-valid` item reads only validation errors, so those 817 skills flip FAIL→PASS on it with no audit-side change. Regression tests in `tests/test_field_classification.py`, including a guard that the new warning text can never collide with the substring `audit.py` uses to bucket the naming-convention warning.

### Deferred, not forgotten
- All four `audit-gap` issues from the audit pilot are now closed (#5/#6 in `[2.4.12]`, #4/#7 here) — the label's tracking job is done. One residue observation recorded for honesty, not action: pilot-era audit runs wrote this pipeline's own scan markers into some gitignored vendored clones (e.g. obra's `brainstorming`), so auto-detection classifies those specific local copies as first-party; that's the detector working as specified on residue our own tooling left, `--source third-party` covers it, and historical pilot artifacts stay untouched by convention.

## [2.4.12] - 2026-08-20

### Fixed
- **`validate.py`'s Claude Code field allowlist was missing eight officially documented frontmatter fields** — resolving [#5](https://github.com/EONRaider/SkillArtisan/issues/5), which had been deliberately deferred pending confirmation that `agent:` is a real Claude Code field rather than a bespoke author convention. It is real: the official frontmatter reference (https://code.claude.com/docs/en/skills.md) documents `agent` as "which subagent type to use when `context: fork` is set" — exactly the usage found in daymade's `competitors-analysis`, the corpus skill that exposed the gap. The same check against the docs showed `CLAUDE_CODE_ONLY_FIELDS` was also missing `arguments`, `disallowed-tools`, `model`, `effort`, `background`, `hooks`, and `shell`, so the fix synced the whole allowlist rather than adding one field. `competitors-analysis` now validates clean (verified against the vendored clone), while Phase 5's recorded true positive — `user_invocable`, an underscore typo for the real `user-invocable` — still hard-errors, pinned in both directions by `tests/test_field_classification.py`. `references/surface-matrix.md`'s Claude Code field table updated to match, keeping its "documents these in full" claim true.
- **`audit.py`'s `description-pushy-imperative` check only recognized the literal substring "use when"** — resolving [#6](https://github.com/EONRaider/SkillArtisan/issues/6), the most heavily corroborated of the four deferred gaps: five independent corpora measured equivalent-but-differently-phrased trigger framing ("use whenever", "use for X, Y, Z", "use during authorized red-team...", "use this skill when...") at incidence rates of ~40% sampled (daymade), 82% (mukul975, 668/817), 55% (glebis), and 8.6% (alirezarezvani, 30/349). The original deferral wanted trigger-accuracy data proving the phrasings perform differently before broadening; no such evidence surfaced across 1,418 skills, and the volume of legitimate skills WARNed for pure phrasing variance settled the call. The match is now a bounded regex (`use [this skill] when/whenever/if/for/during`) rather than an anything-goes loosening — a bare "use" or an obra-style "you MUST use this" with no trigger clause still WARNs, because pushy without saying *when* doesn't inform the trigger decision, and the ≥100-char length gate is unchanged. Also fixed the check's OR-phrased WARN message (flagged in `RESULTS.md` from Phase 7): it now says *which* condition fired — missing framing, under 100 chars, or both — instead of leaving an author like obra's `test-driven-development` ("Use when" present, 72 chars) to guess. Regression tests: `tests/test_description_framing.py`.

### Deferred, not forgotten
- [#4](https://github.com/EONRaider/SkillArtisan/issues/4) (third-party-source mode) and [#7](https://github.com/EONRaider/SkillArtisan/issues/7) (framework-mapping field family) are the next release's scope — designed together, since #7's "known third-party metadata" concept is the validate.py half of #4's third-party awareness.

## [2.4.11] - 2026-08-20

### Added
- **Sixth and final real-world audit corpus: all 14 skills in `obra/superpowers`** (Phase 7, closing the approved Phases 4-7 roadmap). **1,418 real-world skills now audited across six independently-sourced corpora, with the roadmap complete.** Applied Phase 6's re-verify-against-the-real-clone rule even though nothing suspicious was expected — found nothing to correct, the one source in the whole roadmap that needed no correction, which is itself a useful confirmation that the rule costs little to apply even when everything is already right.
- **Zero new bugs, zero new gaps** — every flagged item in this corpus was either an already-tracked pattern (issue #6's phrasing gap) or a correctly-calibrated low-severity WARN. Reported as the legitimate, expected outcome the approved plan's own cost-expectations section anticipated ("don't expect a new bug every phase... evidence the known classes are now well-covered"), not a failed search.
- **Closing tally for the whole real-world audit pilot** (Phases 1-7): 1,418 skills audited across six corpora, nine confirmed bugs found and fixed with regression tests (91 tests passing), a reserved-name true positive corroborated 14 times with zero exceptions, and four deferred gaps tracked as GitHub issues with cross-phase corroboration. Full breakdown: `benchmark/audit-pilot/RESULTS.md`'s closing section; forward-looking guidance for any future seventh source: `benchmark/audit-pilot/SCALING.md`'s "Recommendation — roadmap complete".

## [2.4.10] - 2026-08-20

### Added
- **Fifth real-world audit corpus: 349 skills in `alirezarezvani/claude-skills`** (Phase 6 of the approved Phases 4-7 roadmap). **1,404 real-world skills now audited across five independently-sourced corpora.** This phase's own required extra verification step (an exhaustive, not sampled, check of a flat-vs-nested duplicate-content pattern found during planning) surfaced two real discovery-logic problems and one real audit finding, all documented in `benchmark/audit-pilot/RESULTS.md`'s Phase 6 section.
- **A fourteenth reserved-word "claude" instance** (`claude-coach`), a genuine YAML syntax error caught correctly in a third-party skill's own frontmatter, a third `directory name must match skill name` instance, and 12 confirmed-broken cross-references — all real, all correctly flagged, none requiring a tool fix.

### Fixed
- **`_common.find_skill_dirs` (shared by `audit.py bulk` and `dedup_search.py`, not just this pilot) silently followed symlinks** — a previously-latent bug, first exposed by this corpus, which symlink-mirrors every skill into four cross-tool directories (`.codex/`, `.gemini/`, `.hermes/`, `.vibe/`) for compatibility with other agent products. Unfixed, discovery inflated from ~350 real skills to 1,140+, the same skill re-counted once per mirror. Fixed by skipping any match reached through a symlink at any level. Also extended with a fifth discovery pattern (`category/plugin/skills/name/SKILL.md`) after an exhaustive check found 113 of 125 skills at that depth have no flat-layer counterpart at all — genuinely unique content, not redundant packaging. Regression tests added: `tests/test_common_find_skill_dirs.py`.
- **A new content-hash dedup step in `aggregate_findings.py`** (`dedup_by_content`) — 12 of the 125 nested-layer skills found above turned out to be byte-identical repackagings of a flat-layer sibling; auditing both would silently double-count them without changing a single finding. Deliberately kept out of `find_skill_dirs` itself (a content judgment, not a structural one).
- **This project's own planning-session skill-count estimate for this repo was wrong, independently of the symlink bug above.** A GitHub tree-API check (used before cloning, to plan Phase 6) filtered tracked paths by `.path | endswith("SKILL.md")`, which doesn't distinguish a real file (git blob mode `100644`) from a tracked symlink (mode `120000`) — inflating the estimate to "~672 real skills" when the true count, verified against the actual local clone, is 349, close to the repo's own self-reported "362." Corrected in `benchmark/vendored/README.md` and everywhere the stale figure had propagated. New standing lesson for Phases 4-7 (now just Phase 7): an API-based verification pass is not the same claim as checking against the real, cloned filesystem — re-verify headline numbers locally once a repo is actually cloned.

### Deferred, not forgotten
- [#6](https://github.com/EONRaider/SkillArtisan/issues/6) (use-when phrasing) gained a fifth data point, reported honestly as *lower* incidence (8.6%) rather than escalating further — confirms the gap's magnitude is corpus-dependent.
- [#5](https://github.com/EONRaider/SkillArtisan/issues/5) (unrecognized fields) gained corroboration from a different angle: several distinct custom-metadata clusters in this corpus, none as large as #7's pattern, but strengthening the case that "skills carry custom non-portable metadata" is a common pattern worth a general answer.

## [2.4.9] - 2026-08-20

### Added
- **Fourth real-world audit corpus: all 111 skills in `glebis/claude-skills`** (Phase 5 of the approved Phases 4-7 roadmap), a solo-maintainer knowledge-work/personal-automation corpus. **1,055 real-world skills now audited across four independently-sourced corpora.** Ran unchunked (small enough not to need it) — first real-world exercise of the per-skill exception handling added ahead of Phase 4: 3 skills (`daydream`, `insight-extractor`, `thinking-patterns`) have `SKILL.md` files with no YAML frontmatter at all, correctly erroring out individually (documented `audit.py` exit code 4 behavior) rather than crashing the batch or being silently skipped — confirmed-correct behavior, not a bug.
- **A useful contrast with Phase 4**: the same `security-pattern-checks` categories that were mostly confirmed-but-unfixable false positives on Phase 4's deliberately-crafted security-education examples turned out to be genuine true positives here — the author's own real home directory (`/Users/glebkalinin/Brains/brain`) hardcoded across multiple skills (22 occurrences in one skill alone), real blocking `input()` calls in an interactive CLI tool, real `pickle.load()` use for credential caching. Evidence these checks track real content correctly rather than being inherently noisy; which result you get depends on the corpus's genre. Full detail: `benchmark/audit-pilot/RESULTS.md`'s Phase 5 section.

### Fixed
- **`path-references-exist` had a fourth false-positive mechanism**, on top of the two already fixed in `[2.4.6]`/`[2.4.7]`: a `~/`-prefixed cross-skill dependency reference (`[calendar-sync](~/.claude/skills/calendar-sync)`, documenting that a skill expects a companion skill installed elsewhere on the user's machine) was treated as a broken relative link into the skill's own directory. Fixed by adding `~` to `SKIP_PREFIXES` alongside the existing `http://`/`https://`/`mailto:`/`#` exclusions — a `~/`-prefixed target can never be a relative path within the skill's own bundle, so this carries no risk of masking a real broken intra-skill link. Verified: the triggering skill now passes, and a full corpus re-run confirmed 100% PASS afterward. Regression test added to `tests/test_validate.py`.

### Deferred, not forgotten
- [#6](https://github.com/EONRaider/SkillArtisan/issues/6) (use-when phrasing) gained a fourth data point: 55% incidence in this corpus, between Phase 4's 82% and Phase 3's ~40% sample — three real-world corpora now show "use for"/"use during"/"use whenever" as a common, legitimate alternative phrasing, strengthening the case this is worth an actual fix.

## [2.4.8] - 2026-08-20

### Added
- **Third real-world audit corpus: all 817 skills in `mukul975/Anthropic-Cybersecurity-Skills`** (Phase 4 of the approved Phases 4-7 roadmap), a single-domain cybersecurity/forensics corpus, independently verified (license, pin, structure) before vendoring — see `benchmark/audit-pilot/README.md`'s "Sources and pins" and `benchmark/vendored/README.md`. **944 real-world skills now audited across three independently-sourced corpora.** The reserved-word naming true positive holds; this corpus mostly uses a different naming convention so didn't add new instances, but the checklist itself continued firing correctly throughout.
- **`aggregate_findings.py` hardened for large-corpus resilience**, ahead of this phase, at the user's explicit request: catches any exception per-skill (not just the two originally anticipated) so one skill's crash can never sink an entire run again, the way Bug #3 did in Phase 3; adds `--start`/`--end` for chunked, resumable auditing of a single large source, and a `--merge` mode to recombine chunk outputs. Paid off immediately — the first unchunked attempt against this corpus hit a real shell-level timeout partway through (real `gitleaks` cost against larger reference content, not a hang), and because results were already split into nine ~100-skill chunks persisted to disk immediately, zero results were lost. Smoke-testing the new chunking against known-good mattpocock data also caught and corrected a real documentation error: `setup-ts-deep-modules` had been mis-recorded as an unfixed `path-references-exist` false positive in `[2.4.7]`, when it was actually already fixed as a side effect of that same release's inline-code-span fix — corrected in `RESULTS.md` and `[2.4.7]`'s entry above.
- **Deferred-item tracking moved to GitHub Issues**, labeled `audit-gap` (`gh issue list --label audit-gap`) — no such consolidated mechanism existed before; the only prior record was scattered `### Deferred, not forgotten` CHANGELOG sub-sections with no single view. Opened #4 (third-party-source mode), #5 (`agent:` field), #6 (use-when phrasing), cross-linked both ways with `RESULTS.md`/`CHANGELOG.md`. New gap from this phase, #7: **a whole family of framework-mapping frontmatter fields** (`mitre_attack`, `nist_csf`, `domain`, `subdomain`, `tags`, `version`, `author`, plus optional `d3fend_techniques`/`mitre_f3`/`atlas_techniques`/`nist_ai_rmf`) is unrecognized by `validate.py`, FAILing **817 of 817** skills in this corpus on a consistent, well-structured taxonomy — not sloppy authoring, the same class of gap as #5 but at corpus-defining scale.
- **A methodological finding as real as a bug fix**: investigated four high-volume false-positive-shaped categories in this corpus (fake-but-realistic credentials tripping `security-gitleaks-clean`, 27 skills; fictional forensic evidence paths tripping `absolute-user-path`, 34 skills; real Windows-native attack-tool syntax tripping `forward-slash-paths-only`, 84 skills; a malware-scanner's own pattern-description text tripping `dangerous-code-pattern`, 7 skills) and deliberately left all four unfixed — every one is a genuine, confirmed match on text that's actually present, and "fixing" any of them would mean teaching a pattern-matcher to distinguish "realistic-looking fake" from "real," which risks silently weakening genuine secret/danger detection far more than the false positives cost. Not every false positive should be fixed; `benchmark/audit-pilot/RESULTS.md`'s Phase 4 section has the full reasoning per category.

### Deferred, not forgotten
- Two items carried over from `[2.4.6]`/`[2.4.7]`, now tracked as issues instead of only CHANGELOG prose: third-party-source mode ([#4](https://github.com/EONRaider/SkillArtisan/issues/4)), unrecognized `agent:` field ([#5](https://github.com/EONRaider/SkillArtisan/issues/5)). The use-when phrasing gap ([#6](https://github.com/EONRaider/SkillArtisan/issues/6)) gained strong corroboration this phase — 668 of 817 skills (82%) use equivalent-but-differently-phrased trigger language, well above Phase 3's ~40% sampled rate.
- **New this phase**: the framework-mapping frontmatter field family ([#7](https://github.com/EONRaider/SkillArtisan/issues/7)) — a stronger candidate for a real fix than #5 was, given its scale and internal consistency, worth prioritizing alongside #4's third-party-source-mode work.

## [2.4.7] - 2026-08-20

### Added
- **Second real-world audit corpus: all 92 skills in `daymade-claude-code-skills`** (already vendored/pinned in this repo, zero new sourcing decisions), plus `benchmark/audit-pilot/aggregate_findings.py` — a new tool that groups audit findings by checklist item across one or more source directories, flags boilerplate-shaped items automatically, and narrows a large run down to a review queue worth an actual source read. Verified against the mattpocock run before trusting it on a new corpus: reproduces that pilot's own 4-skill review queue exactly. 127 real-world skills now audited in total across two independently-authored corpora. The reserved-word naming true positive from `[2.4.6]` corroborated 10 more times (13 of 13 "claude"/"anthropic"-named skills across both corpora sit outside their author's own shipped/mature list). Full findings, the scaling-readiness assessment that led into this run, and the cost data behind it: `benchmark/audit-pilot/RESULTS.md` and `SCALING.md`.

### Fixed
- **`audit.py`'s `check_evals_present` crashed outright (`AttributeError`) on a real `evals.json` shape** — a bare JSON list of eval cases, not this project's `{"evals": [...]}` wrapper. Found running the new aggregation tool against `daymade-claude-code-skills`' `github-sensitive-data-cleanup`, whose bare-list `evals.json` this project's own `benchmark/corpus/github-sensitive-data-cleanup/meta.json` had already documented adapting, months before this crash in `audit.py` itself was found. This violated the script's own documented contract ("Exit codes: 0 report generated ... regardless of pass/fail") and would have crashed `audit.py bulk` too, not just the new tool. Fixed by accepting both the bare-list and wrapped-object shapes, and failing cleanly (not crashing) on anything else. Regression test: `tests/test_audit.py`.
- **`references-toc-for-long-files` only recognized the literal phrase "table of contents"** — the single highest-impact false positive found across both real-world pilots. FAILed 59 of 92 daymade skills on first run; most had a real, working `## Contents` heading with a genuine anchor-link list, just not spelled "Table of Contents". Fixed by also accepting `## Contents`/`## TOC`/`## Index` headings; verified the fix didn't touch the genuine gaps (spot-checked skills still correctly FAIL when no TOC exists at all). Regression test: `tests/test_audit_references_toc.py`.
- **`blocking-interactive-input` (from `[2.4.6]`'s fix) still matched English prose inside Python docstrings**, not just `#` comments — two more real hits (`excel-automation`, `asr-transcribe-to-text`) with the exact same root cause as the bug fixed last release, in a shape the `#`-only fix didn't cover. Fixed properly this time: strips all triple-quoted-docstring line ranges per `.py` file before applying the pattern, not just comment lines. Regression test added to `tests/test_security_scan.py`.
- **`path-references-exist` (from `[2.4.6]`'s fenced-code-block fix) still matched markdown-link syntax inside inline single-backtick code spans** — three more real hits (`docs-cleaner`, `meeting-minutes-taker`, `youtube-downloader`), one of them a skill's own cautionary example of exactly this mistake (*"Do NOT create markdown links to files that don't exist (e.g., `[doc.md](reviewed-document)`)"*, flagged as if it were the mistake itself). Fixed by stripping inline code spans the same way fenced blocks already were; rerunning the aggregator across the full 92-skill corpus afterward confirmed `path-references-exist` at 100% PASS — every instance found across both pilots was one of these narrative-example shapes, none was a real broken link. Regression test added to `tests/test_validate.py`.

### Deferred, not forgotten
- **A real frontmatter field, `agent:`, that SkillArtisan's spec knowledge doesn't recognize.** `competitors-analysis` (daymade) uses `context: fork` + `agent: general-purpose` together, a coherent real pattern, but `agent` isn't in `validate.py`'s `CLAUDE_CODE_ONLY_FIELDS` allowlist. Not fixed — no confirmation yet that this is an actual Claude Code-recognized field versus a bespoke author convention, and allowlisting the wrong thing is worse than leaving it flagged. `benchmark/audit-pilot/RESULTS.md`. Tracked: [#5](https://github.com/EONRaider/SkillArtisan/issues/5).
- **Third-party-source mode**, carried over from `[2.4.6]` — now confirmed at 127-skill scale, not just 35. `evals-present`/`security-scan-marker-current`/`lifecycle-classified` still FAIL on effectively every third-party skill regardless of quality. Tracked: [#4](https://github.com/EONRaider/SkillArtisan/issues/4).
- **`description-pushy-imperative`'s exact-phrase "use when" matching** — a softer, sampled-not-verified observation from Phase 3: roughly 16 of 39 sampled WARNs use equivalent-but-differently-phrased trigger language ("use whenever", "use for") instead of the literal checked phrase. Not fixed — no evidence either phrasing performs differently in actual trigger-accuracy testing, only that they're semantically close; a softer case than the four bugs above, worth revisiting if it turns out to matter. Tracked: [#6](https://github.com/EONRaider/SkillArtisan/issues/6).

## [2.4.6] - 2026-08-20

### Added
- **First real-world validation of `audit.py` against skills it didn't author** — every prior QA pass tested the authoring/eval engine on a synthetic corpus; the audit path itself had never been run against skills that exist in the wild. Audited all 35 skills in `mattpocock-skills` v1.2.3 (`github.com/mattpocock/skills`), grading every finding by hand against the actual source rather than trusting the tool's own report. Confirmed the checklist tracks something real: the reserved-word naming check (`frontmatter-valid` flagging "claude"/"anthropic" in a skill's `name`) predicted real-world exclusion in 3 of 3 cases — every "claude"-named skill in the repo sits outside the author's own shipped list, without being told about it in advance. Full findings, methodology, and cost data in `benchmark/audit-pilot/`.

### Fixed
- **`security_scan.py`'s `blocking-interactive-input` check matched ordinary English prose, not just real blocking calls** — `INTERACTIVE_INPUT_PATTERNS`'s `input(` pattern flagged a comment reading "visible input (non-secret)" as a HIGH-severity finding, at the wrong line number, on a skill (`wizard`) whose entire documented purpose is walking a human through blocking interactive prompts. Found via the real-world audit pilot above, not the existing synthetic corpus. Fixed by skipping full-line comments before applying the pattern. Regression test: `tests/test_security_scan.py`.
- **`validate.py`'s `path-references-exist` check matched markdown-link-shaped text inside fenced code-block documentation examples as if they were real file references** — `wayfinder`'s SKILL.md includes a fenced `markdown` template example containing a worked-example link, `[<closed ticket title>](link)`; the literal word "link" isn't a path, but the check flagged it as a missing file. Fixed by stripping fenced code blocks before scanning for links. Regression test: `tests/test_validate.py`. A second instance (`setup-ts-deep-modules`) was originally reported here as a deliberately-unfixed inline non-fenced case — that was a misread on my part; it's actually the same inline-code-span shape `[2.4.7]`'s Round-2 fix below covers, and was already fixed by it. Corrected in `benchmark/audit-pilot/RESULTS.md`.

### Deferred, not forgotten
- **Third-party-source mode for `audit.py`.** Three checklist items (`evals-present`, `security-scan-marker-current`, `lifecycle-classified`) FAILed on all 35 of 35 real-world skills audited above, with zero exceptions — each checks for a SkillArtisan-specific artifact no skill authored outside this pipeline would ever have, which dilutes the checklist pass-rate number and would read as tone-deaf to an outside author. A real scope decision (reframe vs. hard-FAIL for third-party audits), not a one-line patch — left for a future release.

## [2.4.5] - 2026-08-20

### Fixed
- **Root `README.md` was a bare repo map, pointing visitors to `skill-artisan/README.md` just to learn what SkillArtisan does** — but it's the first thing anyone sees, both on the repo landing page and (as of `v2.4.4`'s GitHub Marketplace listing) on the Marketplace page too, which pulls its "About" content from the root README specifically. Verified live: the published Marketplace listing was showing the thin map text, not the actual pitch. Restructured across three files: root `README.md` now carries the full front door (intro, Why this exists, Install, Quick start, GitHub Action, Security, plus short Contributing/Status/Credits sections); `skill-artisan/README.md` keeps the `skill-creator` comparison table and "Verified against `skill-creator`'s current source" as deeper positioning/proof content, plus Quick start and Security as local reference for anyone browsing the installed plugin's own files; a new `CONTRIBUTING.md` holds what's genuinely dev-facing only — repo layout rationale, the plugin's internal architecture, and how to test the eval engine's subagents without a real install (GitHub auto-links `CONTRIBUTING.md` from new issues/PRs, making it the natural home rather than an invented redirect target). Re-verified live after the fix: the Marketplace listing now renders the full pitch.

## [2.4.4] - 2026-08-20

### Fixed
- **`action.yml`'s `description` (~290 chars, a multi-line paragraph) exceeded GitHub Marketplace's 125-character limit for Action descriptions** — found live, navigating to the actual "Edit release" page for `v2.4.3` ahead of publishing: the panel read "Your action.yml needs changes before it can be published... Description must be less than 125 characters." Nothing in the repo's own tooling would have caught this before an actual publish attempt failed; `name`, `icon`, `color`, and the README all passed the same validation with no complaints. Shortened to a single 119-character line covering the same two-mode pitch. Added `skill-artisan/tests/test_action_yml.py` (2 tests) as a regression guard — parses the top-level `description:` key (careful to skip the indented per-input `description` fields) and asserts it stays under 125 chars, so a future edit can't silently grow it back past the limit.

## [2.4.3] - 2026-08-20

### Fixed
- **GitHub reported `licenseInfo: null` for the repo** — the only `LICENSE` file lived at `skill-artisan/LICENSE`, not the repo root, and GitHub's license detection (plus GitHub Marketplace's Action-publishing eligibility check) reads the root copy specifically. Added a root-level `LICENSE` (identical MIT text). This is a deliberate exception to the README/CHANGELOG "no duplication" policy: license text is static boilerplate with no drift risk, and the two copies now serve genuinely different consumers — root for GitHub's own tooling, `skill-artisan/` for what ships with the installed plugin. Verified: `gh repo view` now reports `{"key":"mit","name":"MIT License"}` instead of `null`. Also verified live, end to end, against the `v2.4.2` tag ahead of Marketplace publishing: both the report-only and fix-PR paths ran clean (report-only audited all 3 fixtures correctly; fix-PR opened a real additive-only PR from a genuine audit finding, then closed/cleaned up as a smoke test).

## [2.4.2] - 2026-08-20

### Fixed
- **`model-triggered-fixture` and `user-invoked-fixture` (test fixtures under `tests/fixtures/`) carried three audit FAIL items** (`evals-present`, `security-scan-marker-current`, `lifecycle-classified`) purely from being minimal — nothing about them needed to stay incomplete. Added real eval cases (3 each) and a proper lifecycle classification line (`references/lifecycle.md`'s convention: both "lifecycle" and "timelessness" keywords in the SKILL.md body, not a bare frontmatter field) to both. `security-scan-marker-current` can't be fixed durably via a commit — `.security-scan-passed` is gitignored by design (confirmed: even the real `creating-skills/` skill only ever has a local, uncommitted one), so any fresh checkout, including every CI run, reports it missing regardless — that's the marker working as intended, not a fixture defect. Verified live via `self-test-action.yml` against a fresh checkout: `model-triggered-fixture` went from 12/15 (80%) to 14/15 (93%), `user-invoked-fixture` from 13/16 (81%) to 15/16 (94%) — the remaining gap on each is the marker item plus `security-gitleaks-clean` counting as `MANUAL` rather than `PASS` on the GitHub-hosted runner, which doesn't have `gitleaks` installed (an environmental difference, not a content defect; both fixtures score 100% locally with `gitleaks` present and a fresh `security_scan.py` run). `tests/fixtures/gha-fix-pr-smoke-test` is deliberately left as-is — its incompleteness is the entire point of `self-test-action-fix-pr.yml`'s smoke test, not a gap.

## [2.4.1] - 2026-08-19

### Fixed
- **`find_existing_pr`'s idempotency check matched `--state all`, so a closed (unmerged) PR permanently short-circuited every later run for that skill+repo** — the deterministic branch name means the head-branch match never changes, so once a fix-PR is closed without merging, no future audit run could ever propose a fresh fix for that skill again; it would just silently report the dead PR's URL and exit 0. Found live: re-running `self-test-action-fix-pr.yml` against the `v2.4.0` tag, intending to confirm the tagged release's full pipeline end to end, after having closed the previous smoke-test PR as cleanup — the run reported success but had actually taken the idempotent shortcut, never re-exercising commit/push/PR-creation at all. Fixed by filtering to `--state open` only; a closed PR no longer counts as "already exists." Verified live: `self-test-action-fix-pr.yml` re-run against `master` after the fix opened a genuine new PR (`EONRaider/SkillArtisan#2`), not a repeat of the old closed one — same additive-only diff shape as `#1` (a `lifecycle: experimental` line plus `evals/evals.json`), then closed and its branch deleted as cleanup.

## [2.4.0] - 2026-08-19

### Added
- **GitHub Action** (`action.yml`, repo root): any third-party repository can now install SkillArtisan's audit via `uses: EONRaider/SkillArtisan@<ref>` to audit its own skills without a local Claude Code session. Reuses existing infrastructure rather than duplicating it — `scripts/audit.py`'s mechanical checklist for the report, `scripts/pr_execute.py`'s additive-only-hard-refusal/idempotent-branch git-and-gh mechanics for opening PRs. Two new scripts glue it together: `scripts/anthropic_client.py` (a ~100-line stdlib `urllib` wrapper around a single Messages API call — no new dependency, per the project's dependency-free convention) and `scripts/gha_audit.py` (the Action's entrypoint: find skills, run the mechanical audit, and — only when `ANTHROPIC_API_KEY` is supplied — author an additive fix per skill with FAIL items and hand it to `pr_execute.py --execute`). Behavior is gated purely on that secret's presence, not a separate mode flag: report-only without it, fix-PRs with it, `max-skills` capping spend per run. `.github/workflows/self-test-action.yml` exercises the report-only path on demand (`workflow_dispatch`) with no secret required; `.github/workflows/self-test-action-fix-pr.yml` (also `workflow_dispatch` only) exercises the full fix-PR path against a new disposable fixture (`tests/fixtures/gha-fix-pr-smoke-test`) that's deliberately missing evals/lifecycle classification so the audit always has something to fix. Both are manual-only *in this repo specifically*: `self-test-action.yml` initially also triggered on `push`/`pull_request`, which ran SkillArtisan's own audit against itself on every push to master — not what this repo wants, corrected to `workflow_dispatch` only after being caught running that way for real (see run history around 2026-08-19T22:55-23:08 UTC).
- **`skill-artisan/tests/test_anthropic_client.py`, `test_gha_audit.py`, `test_pr_execute.py`** (39 new tests): full coverage of the new Action machinery with zero network calls — `urllib.request.urlopen` is mocked throughout, including retry-on-429/5xx and non-retryable-4xx behavior; the LLM fix-response JSON parser is tested directly against fake response strings (valid, malformed, path-traversal, missing-field); `pr_execute.py`'s previously-untested pure functions (`verify_additive_only`, `branch_name_for`, `find_existing_pr`'s same-repo-vs-fork branch filter, `get_change_status` parsing) now have direct coverage via mocked `git`/`gh` calls. Full local suite: `python3 -m unittest discover -s skill-artisan/tests`.

### Fixed
- **`scripts/pr_execute.py`'s `has_push_access` misdetected push access for the GitHub Action running in this repo's own CI**, causing `ensure_fork` to attempt (and fail) a fork of the repo into itself (`HTTP 403: Resource not accessible by integration`) instead of pushing directly. Found live, triggering `self-test-action-fix-pr.yml` with a real `ANTHROPIC_API_KEY` for the first time: the mechanical audit and the Claude fix-authoring call both succeeded (1 API call, as `max-skills: 1` and the fixture's scoped `skills-path` intended), but the PR-open step failed. Root cause: `viewerPermission` reflects collaborator ACL, not what the ephemeral `GITHUB_TOKEN` can actually do — a workflow's own `permissions: contents: write` grants real push access without making the bot identity a "collaborator." Fixed by adding `is_same_repo`/`normalize_repo_slug` (compares the local clone's `origin` remote against `--upstream-repo`) as a same-repo short-circuit checked before the `viewerPermission` API call, in both `has_push_access` and `ensure_fork`. 14 new tests in `tests/test_pr_execute.py` cover the new helpers and the short-circuit directly (mocked, no live gh calls).

- **`action.yml` never configured a git identity before the fix-PR path tries to commit**, so `pr_execute.py`'s `git commit` failed with "Author identity unknown" on any runner (GitHub-hosted runners ship with no default `user.name`/`user.email`) — affects every consumer of the Action, not just this repo. Found live, on the second real trigger of `self-test-action-fix-pr.yml` (the first fork-detection fix above got the run past forking and into an actual push attempt, which is what surfaced this). Fixed by adding a step that sets `user.name`/`user.email` to `github-actions[bot]`, gated on `anthropic-api-key` being non-empty so report-only runs don't touch git config at all.
- **`create_branch_commit_push`'s push wasn't forced**, so any run after the branch's first-ever push was a guaranteed non-fast-forward rejection — `branch_name_for` is deterministic specifically so re-running resets the same branch (per its own docstring), and `checkout -B` already rebuilds it fresh from the base every time; only `git push` itself was never told to match that model. Found live, on the fourth real trigger: the third run's push had actually succeeded, but that run then failed at `gh pr create` (the repo-permission gap below) before a PR existed, leaving a pushed branch with nothing pointing at it — the fourth run's plain push against that same branch was correctly rejected as non-fast-forward, except the real error never reached the log, because the code unconditionally fell through to an SSH fallback that can't work on a hosted runner (no SSH key) and its unrelated "Permission denied (publickey)" overwrote the actual one. Fixed by forcing the push, and by only attempting the SSH fallback when we're not already pushing to the same repo (where retrying the identical push over SSH could never succeed anyway).
- **Repo setting, not code**: `gh pr create` also failed once with "GitHub Actions is not permitted to create or approve pull requests" — a repository security setting (Settings → Actions → General → Workflow permissions), deliberately left for the user to enable themselves rather than toggled by the agent, since it governs what automation is allowed to do to the repo.

Confirmed live end-to-end after the four fixes above: a fifth trigger of `self-test-action-fix-pr.yml` opened a real PR (`EONRaider/SkillArtisan#1`) with a clean additive-only diff — a `lifecycle: experimental` frontmatter line and a new `evals/evals.json` for the fixture, authored entirely by Claude from the audit's FAIL findings, no manual editing. Closed and its branch deleted immediately after, since it existed only to prove the pipeline works.

## [2.3.1] - 2026-08-19

Row 8 (extended Claude Code frontmatter) documented `disable-model-invocation: true` in `references/surface-matrix.md`, but nothing downstream in the authoring workflow actually branched on it: row 3's pushy/"Use when..." description teaching, the description-optimizer stage, the four-stage self-critique's Discovery step, and `audit.py`'s checklist all treated a user-invoked-only skill (never auto-triggered — invoked exclusively by the user, e.g. via a slash command) exactly like a normal model-triggered one. Surfaced when a real skill built with `disable-model-invocation: true` was pushed through the trigger-optimization machinery and rejected it as unnecessary bulk for a skill that will never be auto-triggered in the first place.

### Fixed
- **`creating-skills/SKILL.md`**: "Capture intent" now asks whether the skill is user-invoked only at intent-capture time rather than leaving it to surface later; "Write the frontmatter" skips the pushy/trigger-context description treatment for such skills in favor of a plain, accurate description; the "Description optimization" stage and the self-critique's "Discovery" substage are now skipped entirely for them, since there's no trigger accuracy to measure when the model never decides whether to invoke the skill.
- **`references/frontmatter-spec.md`**: scoped "The description field, in full" (the pushy/imperative techniques and worked example) to model-triggered skills explicitly, with a pointer to skip straight past it for `disable-model-invocation: true` skills.
- **`references/surface-matrix.md`**: the `disable-model-invocation` field's own table row now states that setting it *skips* the description-optimization stage and pushy-description technique, not merely makes them optional.
- **`scripts/audit.py`**: `description-optimizer-run` no longer sits as a perpetually unresolved `MANUAL` checklist item for `disable-model-invocation: true` skills — it now resolves to `PASS` with a "not applicable" detail (still `MANUAL` for normal skills, verified against a control fixture). `check_description_quality` no longer penalizes such a skill's plain description for lacking "Use when..." framing.

### Added
- **`skill-artisan/tests/test_disable_model_invocation.py`** (11 tests) plus two fixture skills (`tests/fixtures/user-invoked-fixture`, `tests/fixtures/model-triggered-fixture`): regression coverage confirming `audit.py`'s checklist branches correctly on the field (and only on the field — the model-triggered fixture still gets the manual reminder), `validate.py`'s field classification is unaffected, and the doc-gating language in `SKILL.md`/`frontmatter-spec.md`/`surface-matrix.md` is actually present in the source files, not just asserted in this entry. Full suite (15 tests including the pre-existing marketplace-manifest tests) passes; `audit.py report` and `validate.py` were also run directly against both fixtures to confirm the same behavior at the CLI level.

## [2.3.0] - 2026-08-19

The install command in `README.md` had never actually worked: `claude plugin marketplace add <owner>/skill-artisan` pointed at a repo shorthand that doesn't exist (`SkillArtisan` has no hyphen — `git remote -v` confirms `EONRaider/SkillArtisan`), and there was no `.claude-plugin/marketplace.json` anywhere in the repo for `marketplace add` to find in the first place. This closes both gaps and verifies the fix end-to-end rather than trusting the manifest by inspection.

### Added
- **`.claude-plugin/marketplace.json`** (repo root, previously missing entirely): a single-plugin marketplace manifest named `eonraider`, chosen as a shared distribution channel for future plugins beyond this one rather than a name scoped to just this repo. Its one entry (`skillartisan`, `source: "./skill-artisan"`) is what makes `claude plugin marketplace add eonraider/SkillArtisan` resolve to anything at all.
- **`skill-artisan/tests/test_marketplace_manifest.py`**: a new stdlib-`unittest`, zero-dependency regression suite guarding the invariant that just got introduced — `marketplace.json`'s plugin entry and `skill-artisan/.claude-plugin/plugin.json` must agree on `name` and `version`, or `claude plugin install skillartisan@eonraider` breaks silently for every user until someone notices. Verified the suite actually catches drift, not just that it passes: deliberately bumped `plugin.json`'s version without touching `marketplace.json`, reran the suite, confirmed a clear `AssertionError` naming both mismatched values, then reverted.

### Fixed
- **`skill-artisan/.claude-plugin/plugin.json`'s `name` field was `skill-artisan`, but the plugin's actual product name is `SkillArtisan` (installed as `skillartisan`, no hyphen)** — `skill-artisan` is correctly the name of the bundled skill and the on-disk directory, but was also, incorrectly, the plugin's own identity. Renamed to `skillartisan`; grepped `.json`/`.py`/`.md` across the repo first (excluding vendored/benchmark-workspace copies) to confirm nothing else hardcodes the old plugin name — the `skillartisan:creating-skills`-style subagent namespacing seen at runtime is generated from this field at install time, not text that needed chasing down separately.
- **`README.md`'s install instructions used a repo shorthand that would 404.** `<owner>/skill-artisan` assumed a hyphenated repo name; the real repo is `EONRaider/SkillArtisan`. Now reads `claude plugin marketplace add eonraider/SkillArtisan` / `claude plugin install skillartisan@eonraider` (GitHub repo lookups are case-insensitive, so `eonraider/skillartisan` also resolves to the same repo — no separate casing fix needed).

Verified: `claude plugin validate .` passes against the new manifest; a full local-path smoke test (`claude plugin marketplace add /path/to/repo` → `claude plugin install skillartisan@eonraider` → `claude plugin list`) shows the plugin installed and `✔ enabled` at `2.3.0`. Not yet verified: the real `eonraider/SkillArtisan` GitHub shorthand, since that requires pushing this change to the remote first — left as the user's own next step, not done as part of this change.

## [2.2.4] - 2026-08-18

A fifth real defect found in row 15's ported eval engine, this time via direct root-cause investigation into a `benchmark/` regression finding: two corpus skills' description-optimizer runs left every held-out validation query stuck at an identical pass/fail result across all iterations, no matter how differently the description was reworded.

### Fixed
- **`description_optimizer.py`'s train/validation split gave no warning when the validation set was too small to be a meaningful generalization check.** `split_eval_set()` computes a single train/validation split once, with a hardcoded `seed=42`, before any iteration runs; `improve_description()` is then deliberately never shown which validation queries are failing (`blinded_history` strips every `test_`-prefixed key — correct anti-overfitting design). But for a small eval corpus, the one-time holdout can be tiny: on a real corpus skill (`bilibili-source`, 9 should-trigger examples, the tool's own default `holdout=0.4`), the validation split held exactly 3 should-trigger examples — and those 3 specific queries failed identically across all 5 iterations, completely unmoved by materially different description rewrites, because the optimizer had zero visibility into them from iteration 1 onward and the split never rotates. With this few validation examples per class, a single query flipping swings the reported score by 20+ percentage points, and nothing in the tool surfaced this — a user just saw `best_test_score: 3/6` with no indication the run was structurally incapable of doing better. Added `split_validation_warning()`: prints a `WARNING:` to stderr when either validation class has fewer than 5 examples, and surfaces the same message in the returned dict's new `validation_warning` key and in the HTML report's summary panel, so it's visible whether a caller is watching stderr, reading the JSON, or just the report.
- **Confirmed present, unfixed, in `skill-creator`'s own current `run_loop.py`** — identical `split_eval_set()` (same fixed `seed=42`, same `max(1, int(len(trigger) * holdout))` logic, same default `holdout=0.4`), and the identical blinding mechanism, down to the same code comment ("Strip test scores from history so improvement model can't see them", line 194). No minimum-size check or warning there either. Added to the master spec's Confidence Notes and `benchmark/skill-creator-comparison-repro.sh` as Finding 5.

## [2.2.3] - 2026-08-18

A fourth real bug found in row 15's ported eval engine, this time by deliberate hardening rather than incidental discovery during a benchmark run — triggered by investigating why Axis 2 trigger-accuracy scoring (`axis2_trigger_scorer.py`) had `--num-workers` capped at 1.

### Fixed
- **`description_optimizer.py`'s `run_single_query()` published and tore down its synthetic per-query skill directory non-atomically** (`mkdir()` then a separate `write_text()` to publish; `unlink()` then a separate `rmdir()` to tear down), both against the same shared `.claude/skills/` directory every concurrent worker scans at startup. Between the two calls in each pair there's a real window where a concurrent scan can observe a broken entry — a directory with no `SKILL.md` yet, or (for teardown) one that's already been emptied but not yet removed. Fixed by building each entry in a hidden staging directory and publishing/removing it with a single `os.rename()`, which is atomic on the same filesystem: a directory listing during either operation now only ever sees the complete entry or nothing, never a partial one. Verified with a dedicated concurrency stress test (no LLM calls — pure filesystem race, `race_stress_test.py` under scratch) that reproduces the mechanism directly: with a deliberate delay forced into the vulnerable window, the old code exposed a broken entry in ~99% of concurrent scans; the fix brought that to 0%.
- **Correction to prior understanding, not a fix**: this atomic-write race is real and worth fixing, but it turned out not to be the (or the whole) cause of the `--num-workers 4` → 0% trigger-detection collapse originally documented in `axis2_trigger_scorer.py`'s `--num-workers` help text. Live validation after the fix (`debugging-network-issues`, `--num-workers 4` vs. the known-good `--num-workers 1` baseline of 14/16) still dropped to 10/16, with every failure showing 0 triggers across all runs and the log showing every query timing out at 180s — including ones that "passed" only because a timeout reads as not-triggered, which happens to match a should-not-trigger query's correct answer. Sequential runs show the same 100%-timed-out log pattern but sane, varied pass rates, because `run_single_query` correctly latches its `triggered` result from a mid-stream tool-use event well before the session's later work (actually reading/using the skill) blows through the timeout — that part is normal. What's different at 4 workers is that several queries never got that far at all: the tool-call decision itself didn't happen in time. That points to raw resource contention (four concurrent `claude -p` processes competing for CPU/network/API throughput on one machine) as the dominant remaining bottleneck, not a filesystem race — so `--num-workers 1` stays the safe default until that's separately investigated. The fix above is real and shipped regardless; it just isn't the unlock it looked like it might be.
- **Confirmed present in `skill-creator`'s own shipped `run_eval.py`, and worse**: the identical `mkdir()`-then-`write_text()` pattern exists there too (lines 57/68), with more code running between the two calls than SkillArtisan's version had even before this fix — a wider vulnerable window, still unfixed upstream as of this check. See the master spec's Confidence Notes for the running tally of verified skill-creator defects.

## [2.2.2] - 2026-08-18

Three real bugs found and fixed in row 15's eval/description-optimization engine — `description_optimizer.py` and `eval_loop.py` are direct ports of `skill-creator`'s own `run_eval.py`/`run_loop.py`/`improve_description.py`/`generate_report.py` and `aggregate_benchmark.py` respectively (see the master spec's Confidence Notes); `security_scan.py` is `daymade/claude-code-skills`' architecture, not a `skill-creator` port. All three were caught during real production use: authoring and eval-testing 13 new corpus skills through `creating-skills` end-to-end (single-arm regression testing, see the master spec's Best-in-Market Scorecard correction), not by inspection or a dry run.

### Fixed
- **`description_optimizer.py`'s `run_single_query()` returned prematurely on the first non-Skill/Read tool call or first assistant message**, causing false-negative trigger detection instead of watching the full conversation through to the `result` event. Removed the premature `return` statements; the function now only returns from inside the `result`-event branch.
- **`description_optimizer.py`'s default 60-second timeout was too short for real `claude -p` calls**, which observed 2-3+ minutes in practice. Raised to 180s, with a stderr warning on timeout added (the function's plain-bool return type couldn't be changed without breaking other callers, so the timeout itself isn't distinguishable from a genuine negative result to callers — the warning is the mitigation).
- **`eval_loop.py`'s `load_run_results()` crashed on an explicit JSON `null`** in timing/metrics fields (as opposed to a missing key) — a grader legitimately unable to recover timing data writes explicit `null` rather than omitting the key, and `calculate_stats` crashed on `sum([..., None, ...])`. Fixed via a `.get(key) or default` pattern throughout.
- **`security_scan.py --json` mode leaked human-readable text onto stdout after the JSON payload** on clean scans, breaking anything parsing the output as JSON (11 of 12 `--json` scans in the regression run failed to parse for this reason before the fix). That text now routes to stderr when `--json` is set.

## [2.2.1] - 2026-08-16

`pr-execute --execute`'s first live end-to-end test, against a disposable repo (`EONRaider/skillartisan-pr-execute-test`) created specifically for this, with explicit sign-off immediately before the live invocation per the plan. Found and fixed one real bug along the way.

### Fixed
- **`find_existing_pr`'s idempotency check silently found nothing for a same-repo (direct-push, no-fork) target.** `gh pr list --head` takes a bare branch name for a PR whose head is in the same repo, but requires `owner:branch` syntax specifically for a cross-repo (forked) PR — the function used `owner:branch` unconditionally. Surfaced live: after the first `--execute` run genuinely opened a real PR (`.../pull/1`, correctly additive-only — `SKILL.md` +4/-0, `evals/evals.json` +24/-0, zero deletions), a second run against the same skill fell through the idempotency check entirely and attempted a real push, which then failed on an unrelated non-fast-forward error rather than being caught by the check that exists specifically to prevent that attempt. `find_existing_pr` now takes the same `direct_push` signal `ensure_fork` already computes, and picks the correct `--head` format accordingly. Verified: a third run correctly short-circuits to the existing PR's URL, exit 0, no duplicate PR, working tree and current branch both left untouched (confirmed via `gh pr list` and `git status`/`git branch` before and after).
- Confirms the additive-only invariant holds in a real committed diff, not just the dry-run preview: the actual merged file list for `.../pull/1` shows zero deletions.

The disposable test repo and its one open PR are left in place — nothing about this repo needs preserving, but deleting a repository is exactly the class of irreversible action this project's own safety model reserves for the user's own explicit action, not something to do unprompted even for scratch material.

## [2.2.0] - 2026-08-16

Closes row 32's real-execution half. `audit.py pr-plan` has always been plan-only; this adds the actual fork/branch/commit/push/PR mechanics as a separate, explicitly-gated path, per the master spec's requirement that opening a PR against a repo this plugin doesn't own needs explicit human confirmation every time, not once per skill.

### Added
- **`scripts/pr_execute.py`** (row 32, execution half): a new, separate script (not folded into `audit.py`, per the `scripts/` discipline — one script computes reports, this one performs side effects) that takes a local clone with an additive fix already applied as uncommitted changes, and branches/commits/pushes/opens a PR against the stated upstream repo. Deliberately does not author fixes itself — content authoring stays high-freedom, judgment-heavy work done by the orchestrator with normal Read/Edit/Write, the same as any other real fix; this script is scoped to the low-freedom, exact-script mechanics around an already-made change.
  - `--dry-run`: computes and prints the branch name, diff summary, and PR preview with zero git/gh mutation. This is the default-safe mode and was exhaustively tested (see Deferred, above, for what wasn't).
  - `--execute`: performs the real fork/branch/commit/push/PR sequence. No prompt of its own and no auto-confirm flag — the confirmation gate is structural, one layer up in the orchestrator's chat turn, exactly as `creating-skills/SKILL.md` subsection 6 already required before this shipped.
  - **Hard invariant**: refuses (exit 4) if the change set includes any file deletion or rename, checked mechanically against `git status --porcelain` — additive-only is enforced, not left to discretion.
  - **Idempotent**: a deterministic (non-time-stamped) branch name per skill, checked via `gh pr list` before creating anything — a rerun against a skill with an already-open PR returns that PR's URL rather than opening a duplicate.
  - Distinct exit codes throughout (nothing-to-contribute, non-additive refusal, `gh` not installed/not authenticated, fork/push/PR-creation failure, success), mirroring `security_scan.py`'s precedent of distinguishing "the tool itself couldn't run" from "a real problem was found."
- **`scripts/audit.py pr-execute`**: thin wrapper subcommand delegating to `pr_execute.py`, so `audit.py` stays the single entry point for every audit-related subcommand while the git/gh logic lives in its own tiny, separately-runnable module (same reuse pattern as `validate.py`/`security_scan.py` already used by `report`/`bulk`).
- **`creating-skills/SKILL.md`**: subsection 6 rewritten to describe the two-step plan-then-execute flow, and `compatibility` frontmatter now lists `gh` (authenticated) as a requirement for the execution path only — everything else in the skill works without it.

### Fixed
- `audit.py pr-plan`'s and `SKILL.md` subsection 6's prose both said "never forks, commits, or opens anything" in the present tense — true when written, false the moment real execution existed elsewhere. Both revised to describe the actual current split between the plan-only command and the separate, execution-capable one.

## [2.1.0] - 2026-08-16

Closes three of the five `[Unreleased]` items from `2.0.1`: `creating-skills` now has its own eval suite, it's been verified across all three model sizes, and the `agents/*.md` subagent-invocability gap has been directly tested rather than left as an open question. All three real findings below were caught by actually running the thing, not by writing it and assuming it was right.

### Added
- **`creating-skills/evals/evals.json`** (row: `evals-present`, previously FAIL, now PASS — `audit.py report` confirms 16/17, up from 15/17). Six scenarios exercising the Decision Gate directly: a CLAUDE.md-shaped misroute, a ≥80% dedup match, a 50-79% dedup match with an inline-vs-fork call, a genuine compose case, a fork-vs-inline worked pair, and a security-gate packaging refusal. Verified concrete enough to actually grade — not just plausible-looking prose — via a real subagent run against eval 1, scored 3/3 PASS with cited evidence.
- **Multi-model verification of `creating-skills`**, real results, not a claim it "should work": with-skill runs across Haiku, Sonnet, and Opus as executor (1 rep per eval per model, 18 runs total — see the scope note in `[Unreleased]` above for why this isn't the full 5-rep preset). `eval_loop.py aggregate`: **Haiku 83.3%, Sonnet 100%, Opus 100%.** Haiku failed eval 4 (the compose case) specifically — it never searched beyond the default project-local path, never evaluated a real candidate skill's content, and routed to a "hook-based automation" category that doesn't exist anywhere in `creating-skills`' own Decision Gate table, rather than working within the documented gate. Sonnet and Opus both searched the marketplace, correctly identified `claude-md-improver` as a real match, and correctly read past two superficially-plausible candidates (`session-report`, `receipts`) to reject them on their actual content. Opus went further still, independently flagging that the Decision Gate table itself has no row for hooks at all (see Fixed, below) and that eval 5's codebase-audit half has a genuine compose signal with this session's own `code-review`/`security-review`/`simplify` skills.
- **`creating-skills/SKILL.md`'s "Testing and evaluating" section** documents the Task-tool `model` parameter as the actual mechanism for pinning an executor's model during a smoke pass — nothing in this plugin's own scripts does this (`description_optimizer.py --model` only pins `claude -p` subprocess calls, not Task-tool subagent spawns), so it was undocumented until now.
- **The `agents/*.md` subagent-invocability gap, directly tested rather than assumed.** Added a local test marketplace, installed `skill-artisan` from it (`claude plugin marketplace add`/`claude plugin install`, confirmed enabled via `claude plugin list`), then tried `Task(subagent_type: "grader"/"comparator"/"analyzer")` in the same session — all three came back `Agent type '...' not found`. Confirms the registry is fixed at session start and a local install mid-session doesn't refresh it; a fresh session started after install is expected to pick the plugin's agents up (the same discovery mechanism already verified working in `[1.0.0]`), but that half wasn't independently re-confirmed inside this session's own tooling. Documented both paths — installed (`Task(subagent_type: ...)` direct) vs. repo-context (the proven manual fallback, follow each agent `.md`'s process by hand) — in `SKILL.md`'s "Testing and evaluating" section and `README.md`'s Install section, so this isn't a silent trap for the next person developing the plugin itself.

### Fixed
- **`creating-skills/SKILL.md`'s Decision Gate had no routing row for hooks.** Found by Opus's own eval 4 response, independently, while working the actual eval: a request for deterministic per-session automation ("every time I finish a work session") has no home in the gate's routing table — CLAUDE.md, AGENTS.md, MCP, subagent, DESIGN.md, and plugin are all there, but a lifecycle-hook case isn't, even though hooks are real, common automation requests this skill needs to route correctly. Added a row distinguishing "always-true context" (CLAUDE.md/AGENTS.md) from "deterministic action tied to a lifecycle event" (a `settings.json` hook, configured via `update-config`) — the exact distinction Opus had to reconstruct from first principles because the table didn't state it.
- **`creating-skills/evals/evals.json`'s eval 4 named the wrong skill as a compose candidate.** Written from the skill's *name* (`session-report`) without reading its actual content — it's a token/cache/cost usage report from Claude Code transcripts, not a narrative of what changed in a session. Sonnet's and Opus's eval 4 responses both caught this by actually reading the candidate's SKILL.md, which meant the original eval would have failed a *correct* response and rewarded an incorrect one that happened not to name the mismatched skill either. Rewrote the eval's `expected_output`/`expectations` to test the underlying competency (read real candidate content, don't assume from name) rather than hardcoding a specific pair of skill names that turned out to be wrong.

## [2.0.1] - 2026-08-16

Four fixes, all found the same way — by actually running `scripts/audit.py`, `validate.py`, and `eval_loop.py` against real, external data (Anthropic's shipped `skill-creator`, every plugin in the real local marketplace, and a live end-to-end regression-benchmark run) rather than trusting them by inspection. No Gap Table rows affected; these are correctness fixes to existing `2.0.0` behavior, not new capability.

### Fixed
- **`scripts/_common.py`'s `find_skill_dirs` missed the real Claude Code plugin layout.** `audit.py bulk` against the actual installed marketplace (`~/.claude/plugins/marketplaces/claude-plugins-official/plugins`) returned "No skills found" despite 25 real `SKILL.md` files existing there — the function's two-level glob assumed `<plugin>/<skill>/SKILL.md`, but every real installed plugin uses `<plugin>/skills/<skill>/SKILL.md` instead. Added an explicit `*/skills/*/SKILL.md` glob for this shape. Verified: `audit.py bulk` now finds and audits all 25 real skills; `dedup_search.py`, which shares this function, is unaffected on its default paths and now also finds candidates when pointed at the marketplace.
- **`scripts/_common.py`'s frontmatter parsers silently dropped a description with no block-scalar marker.** `audit.py report` against a real installed skill (`math-olympiad`) came back `REBUILD` — its 704-char description read as 0 chars. The description was written as a bare `description:` with the actual quoted value starting on the next line (valid YAML: a plain/quoted scalar can wrap across lines with no `>`/`|` marker at all), a shape neither `parse_skill_md` nor `parse_frontmatter_raw` handled — they only followed a continuation after an explicit block-scalar marker. Factored both parsers onto one shared `_parse_frontmatter_lines` helper that now handles same-line values, block-scalar markers, and bare-multiline values explicitly (the third case's continuation gets its wrapping quotes stripped as syntax; the block-scalar case's does not, since quotes are literal content there). Verified: the false `REBUILD` flips to the correct `upgrade-in-place`; full regression sweep across `skill-creator`, `creating-skills`, `drafting-changelog-entries`, and the dummy test skills showed no change elsewhere.
- **`scripts/validate.py`'s stripped-copy reconstruction could produce invalid YAML.** Synthetic testing of the four YAML block-scalar variants (`>`, `|`, `>-`, `|-`) surfaced a case where a description's raw text happened to start with a literal `"` character — `_common.py` parsed it correctly (the quote is content, not syntax, inside a block scalar), but `run_skills_ref`'s stripped-copy rebuild used naive `f"{k}: {v}"` interpolation, and writing that value straight onto a `description:` line produced ambiguous YAML that `skills-ref` rejected, failing a genuinely valid skill. Fixed by wrapping every reconstructed value in `json.dumps()` — YAML's double-quoted flow scalar is a superset of JSON string syntax, so this escapes any character that could break the line unconditionally, not as a special case. Verified: all four block-scalar variants now validate clean; full regression sweep unchanged.
- **`scripts/eval_loop.py`'s `runs_per_configuration` metadata didn't reflect actual data.** Found while running the audit regression-benchmarking workflow end-to-end for the first time (`drafting-changelog-entries`' old vs. new `SKILL.md`, one real run per eval per configuration via live subagents, graded by hand per the note above, then aggregated): the generated `benchmark.md` claimed "3 runs each per configuration" regardless of how many runs actually happened, since `generate_benchmark()` hardcoded `3` as a fallback whenever `--preset` wasn't passed. Added `observed_runs_per_configuration()`, which derives the count from the real per-(eval, configuration) run tallies in the loaded `grading.json` results instead of assuming it. Verified against both the real 1-run-per-config workspace and a synthetic 3-run-per-config workspace — reports the correct count in both directions. The regression-benchmark run itself confirmed the actual point of the exercise: **zero pass-rate delta** between `drafting-changelog-entries`' pre- and post-Lifecycle-section versions (33.3% both configurations, `eval_loop.py`'s own aggregation), i.e. adding the Lifecycle section didn't regress task performance.

## [2.0.0] - 2026-08-16

Stages 5-6 of the master spec, per the companion build prompt (Prompt B; kept locally rather than in this repo, matching Prompt A's precedent). Read the actual v1 code and the `[1.0.1]` dogfooding entry before designing Stage 6's audit heuristics, per the master spec's own instruction — `scripts/audit.py` reuses `validate.py` and `security_scan.py`'s functions directly rather than re-deriving equivalent checks, and never treats gitleaks/pattern-check absence as a hard error the way `[1.0.1]`'s fix treated silent test collisions as unacceptable: unavailable tooling reports `MANUAL`, not a false PASS or a crash.

### Added
- **Lifecycle framing** (`references/lifecycle.md`, row 14): capability-uplift vs. encoded-preference classification, a scored 0-10 timelessness threshold (≥7 durable), re-benchmarking triggers tied to model upgrades, and a "last verified against model X" convention. `creating-skills/SKILL.md` classifies itself as a working example — encoded-preference, timelessness 9/10, last verified against claude-sonnet-5 (2026-08) — in a new "Lifecycle" section, not just in the reference file.
- **Audit mode** (`scripts/audit.py`, rows 31-32): a `report` subcommand that runs a 21-item checklist pass against an existing skill (frontmatter validity, security-scan cleanliness, evals presence, body-size limits, reference depth/TOC, content hygiene, lifecycle classification, and a heuristic degrees-of-freedom proxy — each PASS/FAIL/WARN, plus four items marked `MANUAL` where no pattern can substitute for a human/LLM read: coherent-unit scoping, multi-model testing, description-optimizer usage, and inline-vs-fork correctness); an upgrade-vs-rebuild decision gate that defaults to upgrade-in-place and only recommends rebuild for a named condition (frontmatter and description both failing together, a capability-uplift skill under the 7/10 timelessness bar, or body size more than double the limit); an institutional-knowledge safeguard that flags `##` section headings with no obvious checklist counterpart for an explicit keep/fold-in/discard decision, never a silent drop; a `bulk` subcommand that runs the same report across every skill found under a directory and prints a collective table (reuses `_common.find_skill_dirs`, promoted out of `dedup_search.py` so both scripts share one implementation rather than two copies); and an optional `pr-plan` subcommand (row 32, lower priority than the rest) that prints an additive-only-changes contribution plan for a third-party skill without ever calling `gh` itself. Regression benchmarking (item 4) is documented as an `eval_loop.py` before/after workflow in `SKILL.md`'s new "Auditing existing skills" section rather than reimplemented — the same division of labor the eval engine already uses, since a standalone script can't spawn the subagents that actually run it.
- **Verified against a real third-party skill, not just a stub.** `audit.py report` was run against Anthropic's actual shipped `skill-creator` (`~/.claude/plugins/marketplaces/claude-plugins-official/plugins/skill-creator/skills/skill-creator` on the machine this was built on) — confirmed a checklist pass rate consistent with the master spec's own Confidence Notes (no frontmatter validation, no security scan, no lifecycle classification, evals present but no gerund-form naming), decision `upgrade-in-place` (its gaps are structural additions, not a broken triggering-logic rebuild case), and real institutional-knowledge candidates surfaced from its actual section headings ("Communicating with the user," "Report structure," "Advanced: Blind comparison," among others). `audit.py bulk` was run against three dummy skills of deliberately varying quality (a clean encoded-preference skill with evals, a mediocre skill with Windows paths and unexplained bare directives, and a broken skill with a name/directory mismatch and no usable description) and produced the expected spread: two `upgrade-in-place`, one `rebuild` — confirming the decision gate's named conditions actually discriminate rather than defaulting to one branch regardless of input. Both dummy fixtures and their location were scratch material for this verification pass, not shipped as part of the plugin.
- **Full Synthesized Checklist walkthrough**, now unrestricted by the v1/v2 exclusions the master spec scoped out at the v1 release checkpoint — every item checked against the complete v1+v2 repo and its file(s) named directly (no separate tracking document; the checklist itself lives in the master spec, not duplicated here).

### Changed
- `scripts/dedup_search.py`'s `find_skill_dirs` moved to `scripts/_common.py` (`audit.py bulk` needed the identical directory-discovery logic; the `scripts/`-discipline rule this plugin already enforces on authored skills — one implementation, not two copies that drift — applies to its own `scripts/` directory too).
- `README.md`'s release-scope section and comparison table updated to reflect v2 as shipped rather than planned.

## [1.0.1] - 2026-08-16

### Fixed
- **`scripts/description_optimizer.py`: trigger-eval self-collision when testing description changes for an already-installed skill.** Found while dogfooding v1 on a real project skill (`drafting-changelog-entries`, added to this repo's own `.claude/skills/` as the v1 dogfood exercise — not part of the shipped plugin). The per-query synthetic test copy gets a randomized name specifically so it won't collide with anything real, but if the skill under test is *also* sitting at its real, natural name in the same project with a near-identical description, the model tends to reach for the naturally-named real one instead of the synthetic candidate — and the harness only counts a trigger against the synthetic name, so every one of those turns silently reads as a miss. Confirmed directly: the exact same query/description pair scored 0/3 with the real skill present, 1/3 with it hidden. `run_eval` now moves the real `.claude/skills/<name>/` aside for the duration of the run and restores it afterward (`try`/`finally`). Verified the restore fires on normal completion; the crash-safety property for an abnormal exit mid-run rests on the `try`/`finally` construct itself rather than on having reproduced an actual kill during testing — worth a closer look before relying on it for anything long-running. Only matters for improving an existing skill — a brand-new skill has nothing installed yet to collide with.

## [1.0.0] - 2026-08-15

Stages 1-4 of the master spec, built and verified against a live `claude -p` session and a real `gitleaks` install (not just documentation) — per the companion build prompt (Prompt A; kept locally rather than in this repo, not published alongside the plugin).

### Added
- **Eval engine**, ported from `skill-creator` with no loss of capability: parallel with/without-skill subagent runs from a clean context, `agents/grader.md`/`comparator.md`/`analyzer.md` subagent roles, the `evals/evals.json` test-case format, benchmark aggregation with mean/stddev/delta (`scripts/eval_loop.py`), an analyst pass flagging always-pass/always-fail assertions, blind A/B comparison, the HTML eval-viewer (`eval-viewer/generate_review.py` + `viewer.html`, Outputs/Benchmark tabs, `--static` mode, `--previous-workspace` diffing), and the trigger-eval review template (`assets/eval_review.html`).
- **Description optimizer** (`scripts/description_optimizer.py`): 60/40 train/validation split (shuffled once, fixed across iterations), 3x runs per query against a 0.5 trigger-rate threshold, up to 5 iterations, selection by validation pass rate. Fixed two real bugs found while verifying this against a live session: the trigger-registration path (`.claude/skills/<name>/SKILL.md`, not `.claude/commands/` — the latter registers as a slash command and never appears in `available_skills`) and a concurrency race between simultaneously-launched `claude -p` subprocesses sharing one project root (fixed with a small launch stagger).
- **Named trial-count presets** (`--smoke`/`--reliable`/`--regression`) and **CI gating** (`--ci --threshold`) in `scripts/eval_loop.py`.
- **The Decision Gate**, in `creating-skills/SKILL.md`: artifact-type routing (skill vs. CLAUDE.md vs. AGENTS.md vs. MCP vs. subagent vs. plugin vs. DESIGN.md), a dedup check with match-confidence routing (`scripts/dedup_search.py` — ≥80% use-existing / 50-79% improve-existing / <50% create-new / compose), and an inline-vs-fork architecture guide with worked examples.
- **`scripts/validate.py`**: wraps the official `skills-ref` validator (verified directly against the real tool) plus a Claude-specific layer — reserved-word check, gerund-naming suggestion, extended-field classification, and path-reference existence checking. `--suggest-compatibility` auto-populates the `compatibility` field per target surface.
- **`references/surface-matrix.md`**: all five surfaces (Claude Code, Claude.ai, Cowork, Claude Tag, Messages API) plus the generic cross-vendor default, extended Claude Code frontmatter fields, and the plugins-cache-vs-source-path gotcha.
- **`scripts/security_scan.py`**: two-tier gitleaks (default gate) + pattern-checks (`--verbose`, educational layer) scanner, verified against a real gitleaks install — including the unsafe-command-interpolation check (row 38) and the blocking-interactive-input / no-documented-cli script-design checks. SHA256 content-hash tamper detection (`.security-scan-passed`, atomic write) blocks packaging on any post-scan edit. `.skillignore`-based packaging exclusion, enforced (not just documented) by `--package`.
- **`references/security-checklist.md`**, **`references/sanitization-checklist.md`** (the AI semantic read-through step, required of the author before publishing their own skill), and **`references/script-design.md`**.
- **`references/writing-philosophy.md`** and **`references/frontmatter-spec.md`**: imperative voice, explain-the-why, coherent-unit scoping, the three-tier degrees-of-freedom heuristic, content hygiene, and the canonical six-field frontmatter spec with the pushy+imperative description technique.
- Plugin manifest (`.claude-plugin/plugin.json`), plugin-root `.skillignore` and `.gitignore`. Verified against `claude plugin validate --strict`, which caught two real issues fixed before release: the `skills` field needed `"./"` (skill directories sit directly at plugin root, not nested under a `skills/` folder) rather than pointing at `creating-skills` itself, and `agents/*.md` needed proper `name`/`description` frontmatter — Claude Code auto-discovers a plugin's `agents/` directory as real invokable subagent definitions, not just reference prose, so `grader`/`comparator`/`analyzer` are now genuinely invocable via the Task tool's `subagent_type`, not only readable as instructions.

### Deliberately out of scope for v1 (not missed — see the master spec's release-split rationale)
- Row 14: lifecycle framing (capability-uplift vs. encoded-preference, timelessness scoring)
- Row 31: bulk/whole-library audit mode
- Row 32: contribute-back / auto-PR mode

All three ship in v2 (Stage 5-6, Prompt B), after v1 has been dogfooded on real skills.
