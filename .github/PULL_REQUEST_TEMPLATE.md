## Summary

<!-- What changed and why. Link any issue(s) this closes. -->

## Test plan

<!-- e.g. `python3 -m unittest discover -s skill-artisan/tests -p "test_*.py"` — N tests pass -->

- [ ]

## Checklist

- [ ] `CHANGELOG.md` entry added (see `.claude/skills/drafting-changelog-entries/SKILL.md` for this project's process — cross-reference the Gap Table where relevant, correct major/minor/patch per this project's local semver interpretation)
- [ ] `plugin.json` version bumped, if this is a release
- [ ] New/changed behavior has a regression test
